# cluster_report.txt — что на самом деле происходит в кластере (СТАРТЕР)
#
# Источники — в data/:
#   pods.txt              kubectl get pods -o wide
#   nodes.txt             kubectl get nodes + сведённые Allocatable
#   events.txt            kubectl get events (отсортированы по времени)
#   describe_c9v7t.txt    подробности «работающего» пода
#   describe_dk3np.txt    подробности падающего пода + его лог
#   deployments.txt       kubectl get deploy + фрагмент yaml и status
#
# Формат: ключ=значение, без пробелов вокруг «=». Строки с # — комментарии.
# Правь копию в artifacts/, потом проверяй:  bash tests/test.sh

# ─────────────────────────────────────────────────────────────────
# 1. Обзор: сколько чего
# ─────────────────────────────────────────────────────────────────

# Всего подов в пространстве имён aurora.
pods_total=

# Сколько подов в статусе Running.
pods_running=

# Сколько подов ГОТОВЫ: в колонке READY левое число равно правому.
pods_ready=

# Сколько подов Running, но НЕ готовы. Это главное расхождение отчёта:
# «Running» означает «контейнер запущен», а не «принимает трафик».
running_not_ready=

# Сколько узлов в состоянии Ready (включая control-plane).
nodes_ready=

# ─────────────────────────────────────────────────────────────────
# 2. Кто не поднялся и почему — у каждого своя причина
# ─────────────────────────────────────────────────────────────────

# Под, который вообще не назначен на узел (STATUS=Pending). Имя целиком.
pending_pod=

# Чего не хватило планировщику: cpu, memory или taint.
pending_shortage=

# Под, чей образ не скачался.
imagepull_pod=

# Тег образа, которого нет в реестре (только тег, без имени образа).
imagepull_tag=

# Под в состоянии CrashLoopBackOff.
crashloop_pod=

# Код возврата его последнего завершения.
crashloop_exit_code=

# Ключ конфигурации, из-за отсутствия которого он падает (из лога).
crashloop_missing_key=

# ─────────────────────────────────────────────────────────────────
# 3. Тот, кто «работает»
# ─────────────────────────────────────────────────────────────────

# Под с наибольшим числом перезапусков.
max_restarts_pod=

# Сколько раз он перезапускался.
max_restarts_count=

# Причина его последнего завершения (из describe).
restart_reason=

# Код возврата при этом завершении.
restart_exit_code=

# Предел памяти, заданный его контейнеру.
memory_limit=

# Код ответа, который отдаёт проба готовности (из событий).
readiness_http_code=

# ─────────────────────────────────────────────────────────────────
# 4. Что это значит для приложения
# ─────────────────────────────────────────────────────────────────

# Сколько реплик заказано в Deployment aurora-api.
desired_replicas=

# Сколько из них доступно по мнению самого Deployment.
available_replicas=

# Сколько подов aurora-api реально принимают трафик (READY 1/1).
serving_replicas=
