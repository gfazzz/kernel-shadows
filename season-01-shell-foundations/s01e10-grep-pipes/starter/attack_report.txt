# ОТЧЁТ ФОРЕНЗИКИ — s01e10 «Океан логов»
# Объект: season-01-shell-foundations/data/access.log (4400 строк, ночь атаки)
#
# Заполни значения справа от "=". Только числа. Строки с # игнорируются.
# Всё выясняется через grep, конвейеры и wc. Угадывать нечего.
#
# ВАЖНО про подсчёт статусов. Строка лога выглядит так:
#   IP - - [дата] "GET /путь HTTP/1.1" 404 1823 "-" "user-agent"
# Число после закрывающей кавычки — это статус, следующее за ним — размер ответа.
# Шаблон ' 404 ' поймает и размер ответа, равный 404. Привязывайся к кавычке:
#   grep -c '" 404 ' access.log

# Всего запросов в журнале
total_requests=

# Запросов, завершившихся статусом 200
requests_200=

# Всех остальных (подозрительных) запросов
suspicious_requests=

# Запросов по каждому статусу
count_401=
count_403=
count_404=
count_500=
count_503=

# Попыток SQL-инъекции: запросы, содержащие "UNION SELECT"
sqli_attempts=

# Запросов от инструментов разведки: в user-agent встречается "scanner"
scanner_requests=

# Запросов за минуту пика атаки — 04/Oct/2025:03:47
attack_minute_requests=
