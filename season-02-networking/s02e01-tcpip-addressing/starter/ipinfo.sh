#!/usr/bin/env bash
#
# ipinfo.sh — s02e01 «Твой адрес в сети» (СТАРТЕР)
#
# Задача: разобрать IPv4-адрес из аргумента:
#   - проверить валидность (4 октета, каждый 0-255);
#   - определить класс (A/B/C по первому октету);
#   - определить тип: loopback (127.*), private (10/8, 172.16/12, 192.168/16),
#     broadcast (255.255.255.255) или public.
#
# Как проходить:
#   1. cp starter/ipinfo.sh artifacts/ipinfo.sh
#   2. заменить TODO
#   3. bash tests/test.sh
#
# Требования среды: bash. Критерии — в mission.md.

set -uo pipefail

ip="${1:?Использование: ipinfo.sh IPv4}"

# TODO 1: разбей ip на октеты. Подсказка: IFS='.' read -r -a oct <<< "${ip}"
#         Проверь, что октетов ровно 4 и каждый — число 0-255.
#         Если невалидно — сообщи в stderr и exit 1.

# TODO 2: выведи "IP: ${ip}".

# TODO 3: определи класс по первому октету (A < 128, B < 192, C < 224, иначе D/E)
#         и выведи "Класс: ...".

# TODO 4: определи тип (loopback / private / broadcast / public) по октетам
#         и выведи "Тип: ...".
echo "IP: ${ip}"
