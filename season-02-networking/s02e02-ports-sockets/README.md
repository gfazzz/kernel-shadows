# s02e02 — Двери: «Что слушает на сервере»

```
Season 2: Networking               Серия s02e02
Локация: Москва 🇷🇺, ЦОД «Москва-1»   День 9 операции, 16:20
Концепт: порты и сокеты — ss, состояние LISTEN, адрес привязки
Тип: Type C — Investigation        Время: ~50 мин      Сложность: ⭐⭐
```

> **Требования среды:** bash, без root, без сети — разведка идёт по снимку в [`data/`](../data/). Критерии — в [`mission.md`](mission.md).

---

## Previously

В [`s02e01`](../s02e01-tcpip-addressing/) ты собрал `ipinfo.sh` и научился читать карту адресов.

---

## Тридцать четыре машины

Дмитрий разворачивает на экране список серверов операции — тридцать четыре штуки, четыре роли.

> — Сколько из них ты можешь описать? — спрашивает Анна. — Не «веб-сервер», а по-настоящему: какие сервисы там подняты и какие из них видны снаружи.
> — Ни одного.
> — Мы тоже. Их разворачивали в три захода за полгода, и последний раз инвентаризацию делали в июле.

Она разворачивает второй экран — журнал внешнего сканирования.

> — Вот это приходит к нам каждый день с разных адресов. Кто-то методично перебирает порты. Не быстро, не шумно — по несколько за час, чтобы не сработали пороги.

> **LILITH:** *«Адрес — это здание. Порты — двери в нём, и их шестьдесят пять тысяч. Большинство должны быть заперты, но „должны" и „заперты" — разные утверждения, и проверить второе можно только изнутри. Тот, кто снаружи, уже проверяет. Успей раньше.»*

## Чему ты научишься

- Различать порт, сокет и соединение — и читать вывод `ss` по столбцам.
- Тому, что решает не порт, а **адрес привязки**: `0.0.0.0` и `127.0.0.1` — это два разных решения о доступе.
- Почему «изнутри» и «снаружи» — разные вопросы, дающие разные списки.
- Метанавыку: **намерение против поведения**. Служба слушает не там, где задумано, а там, где привязана.

Подробнее, с историей и подводными камнями, — в [`theory.md`](theory.md).

---

## Теория: порт, сокет, привязка

### Порт различает сервисы

На одном адресе живёт много программ, и порт говорит, какой из них адресован пакет. Пара `адрес:порт` называется **сокетом**, а полное соединение задаётся четвёркой: адрес и порт источника, адрес и порт назначения.

| Диапазон | Название | Кто занимает |
|---|---|---|
| 0–1023 | well-known | системные сервисы; слушать на них может только root |
| 1024–49151 | registered | прикладные сервисы |
| 49152–65535 | dynamic | исходящие соединения клиентов |

Знакомые: `22` SSH, `53` DNS, `80` HTTP, `443` HTTPS, `3306` MySQL, `5432` PostgreSQL, `6379` Redis, `9200` Elasticsearch.

### `ss` — что слушает прямо сейчас

```bash
ss -tln        # TCP (-t), только слушающие (-l), без резолва имён (-n)
ss -tuln       # плюс UDP
ss -tlnp       # с именами процессов (чужие — только под root)
```

```
State   Recv-Q Send-Q  Local Address:Port   Peer Address:Port
LISTEN  0      128     0.0.0.0:22           0.0.0.0:*
LISTEN  0      128     127.0.0.1:3306       0.0.0.0:*
LISTEN  0      128     [::]:80              [::]:*
```

Флаг `-n` не косметика: без него `ss` попытается разрешить адреса в имена, и на машине с недоступным DNS команда будет висеть секундами.

### Главное — адрес привязки, а не сам порт

Третья строка вывода важнее первых двух:

- `0.0.0.0:3306` — сервис принимает соединения **со всех интерфейсов**, то есть из сети;
- `127.0.0.1:3306` — только с самой машины;
- `[::]:80` — то же, что `0.0.0.0`, но для IPv6.

Один и тот же MySQL на этих двух привязках — это либо база, доступная только приложению на том же хосте, либо база, до которой может дотянуться любой, кто нашёл адрес. Разница в одной строке конфигурации.

> **Дмитрий:** *«Половина утечек, о которых пишут, — это не взлом. Это сервис, который подняли „временно, для теста" на `0.0.0.0` и забыли.»*

---

## Проверка: LILITH задаёт вопросы

**LILITH:** *«`ss` показал `LISTEN 0.0.0.0:6379`. Чем это опасно?»*

<details>
<summary>Подумай 30 секунд</summary>

`6379` — это Redis, который в конфигурации по умолчанию не требует пароля. На `0.0.0.0` он принимает соединения из сети — то есть любой, кто до него дотянулся, может читать и перезаписывать данные, а через несколько известных приёмов и добиться выполнения кода.

Правильная привязка для базы — `127.0.0.1` либо адрес внутреннего интерфейса, закрытого фаерволом. Открытый Redis — один из самых массовых способов потерять сервер, и находят его именно сканированием портов.
</details>

**LILITH:** *«Чем `ss` отличается от `nmap`, если оба показывают порты?»*

<details>
<summary>Ответ</summary>

Точкой зрения. `ss` смотрит **изнутри** машины и показывает, что на ней действительно слушает. `nmap` смотрит **снаружи** и показывает, до чего можно дотянуться через сеть.

Эти списки почти никогда не совпадают: порт может слушать, но быть закрыт фаерволом, а может не слушать вовсе — тогда снаружи он «закрыт» по другой причине. Нужны обе картины, и расхождение между ними — само по себе полезная информация.

Отдельно: сканировать чужие системы без разрешения — не диагностика, а атака. `nmap` разбираем в Season 5, на своей инфраструктуре.
</details>

---

## Задание: пересчитать двери

Это серия-**расследование**: ты не пишешь инструмент, а отвечаешь на вопросы о конкретной машине. Дмитрий снял с боевого сервера `ss -tuln` и положил снимок в [`data/ss_listen_moscow1.txt`](../data/ss_listen_moscow1.txt) — четырнадцать строк, которые надо превратить в понимание.

Артефакт — **заполненный отчёт** `artifacts/ports_report.txt`:

```
listen_lines=14
unique_ports=11
public_ports=22,80,443,3306,4444,9200,33060
loopback_ports=53,5432,6379
exposed_sensitive=3306,9200
suspect_port=4444
```

```bash
cp starter/ports_report.txt artifacts/ports_report.txt
# заполнить значения, добывая каждое командами по снимку
bash tests/test.sh
```

Вопросы в шаблоне и критерии — в [`mission.md`](mission.md). Ни одно значение не нужно угадывать: всё добывается `grep`, `awk`, `sed`, `sort` и `paste` — инструментами, которые ты уже знаешь. Сверка идёт с двумя списками: [`allowed_ports.txt`](../data/allowed_ports.txt) (что согласовано) и [`known_services.txt`](../data/known_services.txt) (что команда разворачивала сама).

Тест ничего не знает про правильные ответы заранее — он вычисляет их из того же снимка. Если данные поменяются, проверка поедет вместе с ними.

<details>
<summary>Подсказка (если застрял на 5+ минут)</summary>

```bash
SNAP=../data/ss_listen_moscow1.txt
awk '/^LISTEN/{print $4}' "$SNAP"                          # колонка адрес:порт
awk '/^LISTEN/{print $4}' "$SNAP" | sed 's/.*://' | sort -un   # номера портов
awk '/^LISTEN/{print $4}' "$SNAP" | grep -E '^(0\.0\.0\.0|\[::\]):'   # только публичные
```

`sed 's/.*://'` берёт всё после **последнего** двоеточия — поэтому работает и на `0.0.0.0:22`, и на `[::]:80`. Список через запятую собирается `paste -sd, -`. Эталон — в `solution/`, открывай после попытки.
</details>

---

## Разбор типичных ошибок

**Резать по первому двоеточию.** `cut -d: -f2` на строке `0.0.0.0:22` сработает, а на `[::]:80` вернёт пустоту: в IPv6-адресе двоеточий несколько. Номер порта — это всегда то, что после **последнего** двоеточия.

**Считать строки вместо портов.** Строк `LISTEN` четырнадцать, портов — одиннадцать: четыре сервиса продублированы для IPv6. Отчёт, где эти числа совпадают, означает, что дубли посчитали как отдельные сервисы.

**Делить привязки надвое.** Кроме `0.0.0.0` и `127.0.0.1` бывает третий случай — привязка к конкретному адресу интерфейса. Экспортёр метрик на `10.50.1.100:9100` не публичный и не локальный: он доступен из внутренней сети ЦОД и больше ниоткуда. Ответ «наружу или на петле» на такой строке неверен в обе стороны.

---

## Debriefing

**Четыре тысячи четыреста сорок четыре**

Одиннадцать портов. Половина — ровно то, что ожидалось: `22`, `80`, `443`, базы на петле, резолвер на `127.0.0.53`.

Дальше начинается интересное. `9200` на `0.0.0.0` — Elasticsearch, поднятый в июле «посмотреть», с полным доступом без пароля. `3306` наружу — legacy-база, которую собирались убрать в августе. И `4444`, которого нет ни в согласованном списке, ни в перечне того, что команда разворачивала сама.

Дмитрий смотрит на эту строку секунд десять.

> — Это не наше, — говорит он наконец. — Четыре четвёрки — дефолтный порт обратного соединения в половине готовых нагрузок. Кто-то запустил там то, чего мы не ставили.

Анна уже фотографирует экран.

> — Не трогай машину. Вообще ничего на ней не делай. Мне нужно снять состояние прежде, чем оно изменится.

```
┌──────────────────────────────────────────────────────────────┐
│                                                              │
│   Двери пересчитаны, лишние найдены. Одна из них — чужая.    │
│                                                              │
│   Но сервер, к которому ты пытаешься подключиться, молчит.   │
│   Он мёртв — или пакет не доходит?                           │
│                                                              │
│   Между тобой и им десяток маршрутизаторов, и любой из них   │
│   может пакет не пропустить. Пора научиться видеть путь.     │
│                                                              │
└──────────────────────────────────────────────────────────────┘
```

**Дальше → [`s02e03`](../s02e03-net-diagnostics/) — `ping` и `traceroute`: жив ли хост и где обрыв.**

Отчёт `ports_report.txt` — инвентаризация поверхности `netshield` (см. [`PROJECTS.md`](../../docs/PROJECTS.md)); в s02e06 к нему добавится карта правил фаервола.

**Навык разблокирован:** порты и сокеты — ss, состояние LISTEN, адрес привязки.

---

## Ссылки и AI-usage

**Справка и книги:**

- `man ss`, `man nmap`, файл `/etc/services` — карта «порт → сервис»
- Nemeth и др., «UNIX and Linux System Administration Handbook» — сетевые сервисы.
- Глубже — [`theory.md`](theory.md) и корневой [`RESOURCES.md`](../../docs/RESOURCES.md).

**Как работать с LILITH:**

Green:

- *«Разбери строку `ss -tln` по колонкам: где адрес, где порт, что такое Recv-Q?»*
- *«Почему сервис на `0.0.0.0` опаснее того же сервиса на `127.0.0.1`?»*
- *«Проверь мой разбор: сколько уникальных портов в этом снимке и почему строк больше?»*

Red:

- *«Заполни отчёт за меня.»* — здесь ценность именно в том, чтобы самому добыть каждое число: после этого строка `ss` перестаёт быть шумом.

---

---

---

---

## Навигация

- ◀ Назад: [s02e01 — Карта на стене](../s02e01-tcpip-addressing/)
- ▲ Сезон: [Season 2: Networking](../README.md) · [весь курс](../../README.md)
- ▶ Дальше: **[s02e03 — Эхо](../s02e03-net-diagnostics/)**
