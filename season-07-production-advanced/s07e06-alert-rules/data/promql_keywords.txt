# Слова PromQL, которые выглядят как имена метрик, но ими не являются.
# Нужны проверяющему скрипту, чтобы отличить «метрика, которой нет» от
# «функция, которую он не знает».
#
# функции агрегации и работы со временем
rate irate increase delta idelta deriv predict_linear resets changes
sum min max avg count count_values stddev stdvar topk bottomk quantile group
histogram_quantile absent absent_over_time present_over_time
abs ceil floor round exp ln log2 log10 sqrt clamp clamp_max clamp_min sgn
time timestamp vector scalar label_replace label_join sort sort_desc
avg_over_time sum_over_time min_over_time max_over_time count_over_time
quantile_over_time stddev_over_time last_over_time
humanize humanizePercentage humanizeDuration humanizeTimestamp
# ключевые слова языка
by without on ignoring group_left group_right offset bool and or unless
