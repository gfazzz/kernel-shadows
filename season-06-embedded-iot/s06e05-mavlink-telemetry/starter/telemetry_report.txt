# telemetry_report.txt — разбор полёта shadow-uav-03 (СТАРТЕР)
#
# Источники — в data/:
#   frame_sample.hex     один сырой кадр с линии, поля не подписаны
#   telemetry.txt        поток кадров: t_ms sysid compid seq msgid поля
#   mavlink_msgids.txt   справочник: номер сообщения -> имя
#   ardupilot_modes.txt  справочник: custom_mode -> имя режима
#
# Формат: ключ=значение, без пробелов вокруг «=». Строки с # — комментарии.
# Правь копию в artifacts/, потом проверяй:  bash tests/test.sh

# ─────────────────────────────────────────────────────────────────
# 1. Разбор одного кадра (data/frame_sample.hex)
# ─────────────────────────────────────────────────────────────────

# Первый байт кадра — магический. Записать как 0xNN, заглавными.
frame_magic=

# Версия протокола, которую этот магический байт обозначает (1 или 2).
mavlink_version=

# Длина полезной нагрузки — из заголовка, в байтах (десятичное число).
payload_len=

# Полная длина кадра в байтах: заголовок + нагрузка + контрольная сумма.
frame_len=

# Номер кадра в последовательности отправителя (десятичное).
frame_seq=

# Идентификатор системы-отправителя.
frame_sysid=

# Идентификатор компонента-отправителя.
frame_compid=

# Номер сообщения (десятичное; в MAVLink 2 это три байта).
frame_msgid=

# Имя сообщения по справочнику data/mavlink_msgids.txt.
frame_msg_name=

# ─────────────────────────────────────────────────────────────────
# 2. Кто говорил на линии и сколько потерялось
# ─────────────────────────────────────────────────────────────────

# Сколько разных систем (sysid) присутствует в потоке.
systems_on_link=

# sysid аппарата (тот, кто шлёт ATTITUDE и GPS_RAW_INT).
uav_sysid=

# sysid наземной станции.
gcs_sysid=

# Сколько кадров аппарата дошло.
uav_frames=

# Сколько кадров аппарата потерялось. Считать по разрывам в seq —
# у КАЖДОЙ системы счётчик свой, и он однобайтовый.
lost_frames=

# Доля потерь в процентах от отправленного, с одним знаком после точки.
loss_percent=

# Сколько разных типов сообщений прислал аппарат.
msg_types=

# ─────────────────────────────────────────────────────────────────
# 3. Что случилось в полёте
# ─────────────────────────────────────────────────────────────────

# Самый долгий промежуток без единого кадра от аппарата, в мс.
link_gap_ms=

# Момент (t_ms), когда GPS впервые перестал давать трёхмерное решение.
gps_lost_ms=

# Минимальное число видимых спутников за полёт.
sats_min=

# Минимальное напряжение батареи за полёт.
battery_min_v=

# Минимальный остаток заряда в процентах.
battery_min_pct=

# Самая высокая серьёзность сообщения STATUSTEXT.
# Внимание: чем МЕНЬШЕ число severity, тем серьёзнее сообщение.
worst_severity=

# Режим аппарата в последнем HEARTBEAT — имя по data/ardupilot_modes.txt.
final_mode=
