# bottleneck_report.txt — где узкое место (ЭТАЛОН)
#
# Метод USE: по каждому ресурсу три вопроса — насколько он занят
# (utilization), есть ли к нему очередь (saturation), были ли отказы
# (errors). Ресурс, у которого утилизация упёрлась в потолок, а очередь
# растёт, и есть узкое место. Остальные надо честно исключить.
#
# Источники — в data/:
#   vmstat.txt          процессор, очередь на выполнение, подкачка
#   iostat.txt          диск: занятость, очередь, ожидание
#   free.txt            память узла
#   nstat.txt           сетевой стек: повторы, переполнение очереди приёма
#   cadvisor.txt        контейнер: троттлинг и память
#   app_metrics.txt     перцентили приложения и пул соединений к базе
#   use_thresholds.txt  пороги, за которыми ресурс считается узким местом
#
# Формат: ключ=значение, без пробелов вокруг «=». Все величины — целые.
# Над каждым значением — как оно добыто.

# ─────────────────────────────────────────────────────────────────
# 1. Процессор узла
# ─────────────────────────────────────────────────────────────────

# Наибольшая утилизация за окно наблюдения: 100 минус наименьший простой
# (колонка id в vmstat).
cpu_util_max_pct=27

# Наибольшая длина очереди на выполнение (колонка r). Это насыщение
# процессора: сколько процессов готовы считать, но ждут своей очереди.
run_queue_max=2

# Наибольшее ожидание ввода-вывода (колонка wa).
iowait_max_pct=0

# ─────────────────────────────────────────────────────────────────
# 2. Память узла
# ─────────────────────────────────────────────────────────────────

# Доля доступной памяти от общей: available, а не free.
mem_available_pct=56

# Сколько ушло в подкачку и вернулось из неё за окно: сумма si и so.
swap_traffic_kb=0

# ─────────────────────────────────────────────────────────────────
# 3. Диск
# ─────────────────────────────────────────────────────────────────

# Наибольшая занятость устройства, целое число процентов.
disk_util_max_pct=12

# ─────────────────────────────────────────────────────────────────
# 4. Сеть
# ─────────────────────────────────────────────────────────────────

# Повторно переданные сегменты за окно.
tcp_retrans=3

# Соединения, не поместившиеся в очередь принятия.
listen_overflows=0

# ─────────────────────────────────────────────────────────────────
# 5. Контейнер: свои пределы, а не пределы узла
# ─────────────────────────────────────────────────────────────────

# Доля стомиллисекундных окон, в которых кончилась квота процессора.
cpu_throttled_pct=1

# Занятая память контейнера в процентах от его предела.
container_mem_util_pct=37

# ─────────────────────────────────────────────────────────────────
# 6. Пул соединений к базе — тоже ресурс
# ─────────────────────────────────────────────────────────────────

# Потолок пула и наибольшая занятость за окно.
pool_max=32
pool_in_use_max=32

# Утилизация пула в процентах.
pool_util_pct=100

# Насыщение: наибольшая длина очереди ожидающих соединение.
pool_waiters_max=47

# ─────────────────────────────────────────────────────────────────
# 7. Куда уходит время
# ─────────────────────────────────────────────────────────────────

# Какую долю 95-го перцентиля ответа занимает ожидание соединения к базе.
wait_share_pct=66

# ─────────────────────────────────────────────────────────────────
# 8. Приговор по каждому ресурсу
# ─────────────────────────────────────────────────────────────────
#
# Ровно два допустимых значения: ok или saturated. Ресурс объявляется
# saturated, если его утилизация выше порога из use_thresholds.txt.
cpu_verdict=ok
memory_verdict=ok
disk_verdict=ok
network_verdict=ok
container_cpu_verdict=ok
pool_verdict=saturated

# Единственный ресурс, оказавшийся узким местом: короткое имя.
# Допустимые: cpu, memory, disk, network, container-cpu, db-pool.
bottleneck=db-pool

# Что именно с ним не так: utilization или saturation.
bottleneck_kind=saturation
