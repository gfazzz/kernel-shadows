# Отчёт по процессам shadow-server-01 — s03e04 (ШАБЛОН)
#
# Заполни значения справа от «=». Пробелы вокруг «=» не важны, регистр важен.
# Строки, начинающиеся с #, — комментарии; их можно не трогать.
#
# Объекты разведки:
#   ../data/ps_shadow-01.txt    — таблица процессов
#   ../data/proc_shadow-01.txt  — снимок /proc по четырём процессам
#
# Проверка:  bash tests/test.sh

# --- Кто настоящий -----------------------------------------------------------
# PID процесса, чей /proc/PID/exe ведёт на штатный /usr/sbin/sshd
real_sshd_pid=

# PID второго процесса, который выглядит в ps так же, но запущен из другого файла
impostor_sshd_pid=
# путь, на который ведёт его exe
impostor_sshd_exe=

# --- Маскировка --------------------------------------------------------------
# PID процесса, у которого исполняемый файл удалён с диска
masquerade_pid=
# путь удалённого файла (без пометки «(deleted)»)
masquerade_exe=
# рабочий каталог этого процесса
masquerade_cwd=

# PID его дочернего процесса (по таблице процессов)
masquerade_child_pid=
# IP-адрес, на который дочерний процесс ходит
beacon_ip=

# --- Состояния ---------------------------------------------------------------
# PID процесса в состоянии зомби
zombie_pid=
# PID его родителя — того, кому и надо слать сигнал
zombie_parent_pid=

# Сколько в снимке потоков ядра (не процессов пользователя)
kernel_threads=

# PID процесса с наибольшей долей процессора
top_cpu_pid=

# Сколько процессов принадлежит учётной записи monitoring
monitoring_procs=

# --- Сигналы -----------------------------------------------------------------
# номер сигнала «завершись сам, прибравшись за собой»
signal_graceful=
# номер сигнала, который процесс не может перехватить
signal_uncatchable=
# номер сигнала, которым демонам традиционно велят перечитать конфигурацию
signal_reload=
