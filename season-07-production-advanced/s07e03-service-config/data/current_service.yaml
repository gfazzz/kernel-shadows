# Черновик Service, который лежит в репозитории с прошлого квартала.
# kubectl apply -f принимает его без единого возражения.
#
# Это НЕ образец. Разбери, почему он ничего не соединяет.
apiVersion: v1
kind: Service
metadata:
  name: aurora-api
  namespace: aurora
spec:
  type: NodePort
  selector:
    app: aurora-api
    pod-template-hash: 7d4f8b6c9
  ports:
    - port: 8080
      targetPort: 80
      nodePort: 31080
