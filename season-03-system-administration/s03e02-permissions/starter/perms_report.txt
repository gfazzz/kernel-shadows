# ОТЧЁТ ФОРЕНЗИКИ — s03e02 «Кто что может»
# Объект: season-03-system-administration/data/perm_audit_shadow-01.txt
#         season-03-system-administration/data/suid_scan_shadow-01.txt
#
# Заполни значения справа от "=". Пути — через запятую без пробелов,
# в алфавитном порядке. Восьмеричные права — как их печатает stat -c '%a'
# (три цифры; четыре, если есть специальный бит). Строки с # игнорируются.
#
# В файлах есть заголовки секций (=== … ===) и шапка — строки прав начинаются
# с "-", "d" или "l", по этому признаку их и отбирают.

# --- обычные права ---------------------------------------------------------
# Восьмеричные права /etc/shadow
shadow_octal=

# Восьмеричные права /etc/sudoers
sudoers_octal=

# Восьмеричные права домашнего каталога dmitry
dmitry_home_octal=

# ФАЙЛЫ (не каталоги), доступные на запись всем, через запятую
world_writable_files=

# КАТАЛОГИ, доступные на запись всем, через запятую
world_writable_dirs=

# Из них — те, где НЕТ sticky bit, то есть любой может удалить чужой файл
unsafe_shared_dirs=

# Приватные ключи SSH, у которых права шире 600, через запятую
exposed_private_keys=

# --- специальные биты ------------------------------------------------------
# Сколько файлов с битом SUID в снимке
suid_total=

# Сколько файлов с битом SGID
sgid_total=

# SUID-файлы за пределами /usr, через запятую.
# Системные SUID живут в /usr/bin и /usr/lib; всё, что вне — повод спросить.
suid_outside_usr=

# Одним путём: SUID-файл, принадлежащий root и лежащий внутри домашнего
# каталога пользователя. Это не бывает случайно.
planted_suid=
