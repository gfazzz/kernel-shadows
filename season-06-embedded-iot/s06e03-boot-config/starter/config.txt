# config.txt — что плата читает до старта ядра (СТАРТЕР, s06e03).
#
# Читает его прошивка SoC, ещё до загрузчика и до ядра. Ошибка здесь не
# даёт сообщения об ошибке — плата просто не поднимается или молчит.
#
# Синтаксис:
#   ключ=значение              обычный параметр прошивки
#   dtparam=имя=on|off         включить/выключить штатный узел дерева
#   dtoverlay=имя,парам=знач   наложить overlay дерева устройств
#   [фильтр]                   всё ниже — только для подходящих плат,
#                              до следующего фильтра; [all] — снова для всех
#
# Задача: собрать конфигурацию загрузки узла shadow-node-07.
# Что включать — смотри в data/sensors.txt и data/board_facts.txt.
# Ничего лишнего: каждая включённая шина занимает ножки гребёнки.

# TODO: включить последовательную консоль — без неё безголовый узел,
#       который не загрузился, не диагностируется

# TODO: включить шины, которые нужны датчикам из data/sensors.txt,
#       и только их (1-wire — overlay с номером ножки из того же файла)

# TODO: безголовый режим — минимум видеопамяти, без заставки и задержки

# TODO: секция только для этой платы (family из data/board_facts.txt):
#       на Pi 4 нормальный UART занят Bluetooth, а консоль висит на
#       mini-UART, чья скорость плавает с частотой ядра — исправить

# TODO: вернуться ко всем платам и дописать общий параметр
#       (иначе всё ниже молча достанется только одной модели)
