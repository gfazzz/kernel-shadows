# ОТЧЁТ РАЗВЕДКИ — s02e02 «Что слушает на сервере»
# Объект: season-02-networking/data/ss_listen_moscow1.txt  (снимок `ss -tuln`)
# Сверка:  season-02-networking/data/allowed_ports.txt      (согласованный allowlist)
#          season-02-networking/data/known_services.txt     (что команда разворачивала сама)
#
# Заполни значения справа от "=". Числа — цифрами; списки портов — через запятую
# без пробелов, по возрастанию (например: 22,80,443). Строки с # игнорируются.
#
# Всё выясняется командами по файлу-снимку. Угадывать нечего.
# Подсказка по разбору: номер порта — это то, что после ПОСЛЕДНЕГО двоеточия
# в колонке Local Address:Port (иначе IPv6-строка [::]:80 разберётся неверно).

# Сколько всего строк LISTEN в снимке (включая дубли IPv4/IPv6)
listen_lines=

# Сколько РАЗНЫХ номеров портов слушает машина
unique_ports=

# Список всех уникальных портов по возрастанию
all_ports=

# Порты, доступные ИЗВНЕ (адрес привязки 0.0.0.0 или [::]), по возрастанию
public_ports=

# Порты, доступные только локально (привязка 127.0.0.x), по возрастанию
loopback_ports=

# Порты, которых нет в allowed_ports.txt, по возрастанию
unexpected_ports=

# Чувствительные порты (СУБД/кеши/поиск), торчащие наружу, по возрастанию.
# Считаем чувствительными: 3306 5432 6379 9200 27017 11211
exposed_sensitive=

# Адрес привязки экспортёра метрик (порт 9100) — одним значением, как в снимке
metrics_bind=

# Порт, который слушает НАРУЖУ и при этом отсутствует в known_services.txt —
# то есть его не поднимал никто из команды. Одно число.
suspect_port=
