#!/usr/bin/env bash
#
# cleanup.sh — убрать найденное и доказать, что убрано (СТАРТЕР).
#
# ВЫЗОВ
#     cleanup.sh [--verify] <корень> <план>
#
#     без ключа  — привести систему к состоянию из плана
#     --verify   — только проверить; ничего не менять
#
# ВЫВОД
#     ITEM <состояние> <действие> <объект>
#     SUMMARY fixed=<n> already=<n> failed=<n>
#
#     состояние: FIXED — привели; ALREADY — уже было так; FAILED — не вышло
#
# КОД ВОЗВРАТА
#     0 — состояние достигнуто, 1 — что-то не достигнуто, 2 — вход не разобран
#
# ГЛАВНОЕ В ЭТОЙ ЗАДАЧЕ
#     Каждая строка плана описывает СОСТОЯНИЕ, а не команду. «Этого файла
#     нет», «этой строки в файле нет», «этой учётной записи нет».
#
#     Отсюда два требования, которые проверяются отдельно:
#       - повторный прогон обязан сказать ALREADY, а не FIXED;
#       - после каждого действия состояние проверяется заново. «Команда
#         выполнена» и «состояние достигнуто» — разные утверждения, и
#         разница между ними — вся суть серии.
#
# ЧЕГО ДЕЛАТЬ НЕЛЬЗЯ
#     Чистить файл целиком. В authorized_keys есть чужой ключ и свой; свой
#     обязан остаться. То же с rc.local и passwd: там законное содержимое,
#     и удаление «на всякий случай» ломает узел.
#
# ФОРМАТ ПЛАНА
#     remove-file  <путь>
#     remove-line  <путь> <подстрока>
#     remove-user  <путь> <имя>

set -uo pipefail

# TODO: разобрать ключ --verify и два аргумента, проверить их.

# TODO: для каждого пункта плана — определить, достигнуто ли состояние.

# TODO: если нет и это не --verify — привести к нему и проверить снова.

# TODO: напечатать строки ITEM, итог и вернуть код.

exit 2
