# ОТЧЁТ РАЗВЕДКИ — s02e06 «Что у нас вообще открыто»
# Объект: season-02-networking/data/ufw_status_moscow1.txt (снимок `ufw status verbose`)
# Сверка:  season-02-networking/data/ss_listen_moscow1.txt  (что реально слушает — из s02e02)
#
# Заполни значения справа от "=". Числа — цифрами; списки портов — через запятую
# без пробелов, по возрастанию. Строки с # игнорируются.
#
# Всё выясняется командами по снимку. Угадывать нечего.
# Читай колонку From: она важнее колонки To. Строки (v6) дублируют правила
# для IPv6 — это те же порты, а не новые.

# Политика по умолчанию для ВХОДЯЩИХ: одно слово — allow или deny
default_incoming=

# Сколько строк с действием ALLOW в снимке (включая дубли (v6))
allow_lines=

# Сколько строк с действием DENY
deny_lines=

# Уникальные номера портов, разрешённые ИЗ ИНТЕРНЕТА (источник Anywhere),
# по возрастанию
open_to_world=

# Уникальные номера портов, разрешённые только из внутренней сети
# (источник — подсеть вида 10.50.0.0/24), по возрастанию
open_to_lan=

# Чувствительные порты (3306 5432 6379 9200 27017 11211), открытые из интернета,
# по возрастанию
exposed_sensitive=

# В снимке есть правило, разрешающее ВСЁ с некоторой подсети — «на время отладки».
# Укажи источник этого правила ровно так, как он записан в колонке From.
blanket_allow_from=

# Порты, которые реально слушают наружу (по снимку ss из s02e02), но НЕ упомянуты
# ни в одном правиле ufw. Через запятую, по возрастанию.
listening_but_unruled=

# Вывод одним словом: защищает ли этот фаервол хоть что-нибудь при такой
# политике по умолчанию — yes или no
firewall_effective=
