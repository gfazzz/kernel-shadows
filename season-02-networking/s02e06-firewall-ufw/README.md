# s02e06 — Стена: «Что у нас вообще открыто»

```
Season 2: Networking               Серия s02e06
Локация: Москва 🇷🇺, ЦОД «Москва-1»   День 13 операции, 04:00
Концепт: правила фаервола — default-deny, чтение ufw status, опасные ALLOW
Тип: Type C — Investigation        Время: ~50 мин      Сложность: ⭐⭐⭐
```

> **Требования среды:** bash, без root — разведка идёт по снимку правил в [`data/`](../data/). Критерии — в [`mission.md`](mission.md).

---

## Previously

[`s02e05`](../s02e05-dns-spoofing-guard/) — `dns_guard.sh`, три отравленных домена.

---

## Три сорок семь ночи

Звонок Алекса выдёргивает тебя из сна за час до будильника.

> — Просыпайся. Нас кладут.

Ты открываешь ноутбук прямо в кровати. Цифры на мониторинге не требуют пояснений:

```
load average: 47.82 (норма 1–2)
входящий трафик: 487 Mbps
CPU: 94%
уникальных источников за минуту: 847
```

> — Пятьдесят тысяч пакетов в секунду, — говорит Алекс. — Ботнет, адреса из Tor-сети, те же выходные узлы, что ты нашёл в Стокгольме.
> — Что уже сделали?
> — Ничего. И это правильно: пока не понимаем, что открыто, любое изменение — стрельба вслепую. Ты сейчас на сервере?
> — Через SSH.
> — Вот это и есть первая мысль, которую надо додумать до конца. Через что ты подключён, то и не должно закрыться, пока ты работаешь.

> **LILITH:** *«Фаервол — последний рубеж, и он же самый простой способ отрезать себя от собственного сервера. Прежде чем что-то менять — прочитай, что там сейчас. Атака идёт минуты, а неверное правило отключит доступ на часы.»*

## Чему ты научишься

- Модели default-deny: как пакет проходит через фильтр и в каком порядке.
- Читать `ufw status` так, чтобы `(v6)` не удваивал список, а `DENY` не попадал в разрешения.
- Отличать порт, открытый в интернет, от открытого только внутрь.
- Метанавыку: **предусматривать откат**. Правило, отрезающее тебя от сервера, применяют иначе, чем остальные.

Подробнее, с историей и подводными камнями, — в [`theory.md`](theory.md).

---

## Теория: правила и порядок

### Default-deny

Фаервол — это список правил и **политика по умолчанию** для всего, что под правила не подошло. Правильная политика для входящих — запрет:

```bash
sudo ufw default deny incoming     # всё входящее запрещено
sudo ufw default allow outgoing    # исходящее разрешено
sudo ufw allow 22/tcp              # ...и точечно то, что нужно
sudo ufw allow 80,443/tcp
```

Обратный подход — «разрешить всё и запрещать плохое» — не работает по арифметической причине: перечислить нужное можно (три-четыре порта), перечислить всё плохое нельзя.

### Как не отрезать себя

Порядок действий имеет значение, и цена ошибки — поездка в дата-центр:

```bash
sudo ufw allow 22/tcp     # СНАЧАЛА разрешить SSH
sudo ufw enable           # и только ПОТОМ включать
```

При включённом `default deny incoming` команда `enable` немедленно применит политику к текущим соединениям. Правило для SSH, добавленное после, уже некому будет добавить.

Приём на случай неуверенности — отложенный откат: перед изменением ставится задание, которое через десять минут вернёт прежние правила. Успел проверить связь и отменить — хорошо; потерял доступ — правила откатятся сами.

### Что видно в `ufw status`

```bash
sudo ufw status numbered
```

```
To                         Action      From
--                         ------      ----
22/tcp                     ALLOW       Anywhere
3306                       ALLOW       Anywhere
6379                       ALLOW       127.0.0.1
```

Колонка `From` важнее колонки `To`. `Anywhere` означает «из интернета», конкретный адрес или подсеть — ограничение. Порт базы данных, открытый на `Anywhere`, — это приглашение, независимо от того, есть ли на ней пароль.

Под `ufw` работает `iptables` с цепочками `INPUT`, `OUTPUT`, `FORWARD`; правила просматриваются сверху вниз, побеждает первое совпавшее. Детали, `nftables`, сохранение правил и то, почему Docker обходит `ufw`, — в [`theory.md`](theory.md).

---

## Проверка: LILITH задаёт вопросы

**LILITH:** *«Почему „разрешить всё и блокировать плохое" — проигрышная стратегия?»*

<details>
<summary>Подумай 30 секунд</summary>

Потому что список нужного конечен, а список плохого — нет. Разрешить три порта можно и нужно; перечислить все адреса ботнета, все будущие уязвимости и все ещё не изобретённые способы обращения к сервису — нельзя.

Default-deny переворачивает задачу так, что незакрытых дыр не остаётся по построению: всё, что не разрешено явно, запрещено. Ошибка в такой конфигурации проявляется как «что-то не работает» — неприятно, но заметно. Ошибка в обратной проявляется как тишина.
</details>

**LILITH:** *«В правилах строка `3306 ALLOW Anywhere`. Чем это грозит прямо сейчас, во время атаки?»*

<details>
<summary>Ответ</summary>

Двумя вещами сразу. Во-первых, это открытая в интернет база данных: перебор паролей, попытки эксплуатации, а при неудачной конфигурации — прямой доступ к данным.

Во-вторых — то, что важно именно сейчас: каждое соединение к MySQL дороже, чем к веб-серверу. Ботнет, долбящий в открытый порт базы, уронит машину быстрее, чем тот же трафик по HTTP.

Место такого сервиса — на `127.0.0.1` либо с правилом `From` для конкретной подсети.
</details>

---

## Задание: прочитать стену

Это снова **расследование**, а не инструмент. Дмитрий снял `ufw status verbose` с того же сервера, чей список портов ты разбирал в [`s02e02`](../s02e02-ports-sockets/), и положил снимок в [`data/ufw_status_moscow1.txt`](../data/ufw_status_moscow1.txt). Твоя задача — понять, что эта стена на самом деле закрывает.

Артефакт — **заполненный отчёт** `artifacts/fw_report.txt`:

```
default_incoming=allow
open_to_world=22,80,443,3306,9200
open_to_lan=5432,9100
blanket_allow_from=185.14.29.0/24
listening_but_unruled=4444,33060
firewall_effective=no
```

```bash
cp starter/fw_report.txt artifacts/fw_report.txt
# заполнить значения, добывая каждое командами по снимку
bash tests/test.sh
```

Вопросы и критерии — в [`mission.md`](mission.md). Один из них требует сопоставить **два** снимка: правила фаервола и список того, что реально слушает. Расхождение между ними — самая ценная находка серии, и ни один из файлов по отдельности её не показывает.

<details>
<summary>Подсказка (если застрял на 5+ минут)</summary>

```bash
FW=../data/ufw_status_moscow1.txt
grep -i '^Default:' "$FW"                          # политика по умолчанию
grep -E '[[:space:]]ALLOW' "$FW"                   # только разрешающие правила
grep -E '[[:space:]]ALLOW' "$FW" | awk '{print $1}' | sed 's|/.*||' | sort -un
```

Номер порта — первое поле строки, `/tcp` отрезается через `sed 's|/.*||'`. Источник смотрится в конце строки. Эталон — в `solution/`, открывай после попытки.
</details>

---

## Разбор типичных ошибок

**Смотреть на порты, не посмотрев на политику по умолчанию.** Строка `Default: allow (incoming)` обесценивает весь список разрешений: всё, что не запрещено явно, и так открыто. Отчёт, где перечислены аккуратные `ALLOW`, но не отмечена политика, описывает несуществующую защиту.

**Считать строки правилами.** Тринадцать строк `ALLOW` дают восемь портов: четыре сервиса продублированы для IPv6. Число строк и число закрытых сервисов — разные величины.

**Читать только колонку `To`.** `3306 ALLOW 10.50.0.0/24` и `3306 ALLOW Anywhere` — правила с противоположным смыслом. И отдельно стоит правило, где в колонке `To` вообще не порт, а `Anywhere`: оно разрешает с указанной подсети всё сразу, и заметить его можно, только читая строку целиком.

---

## Debriefing

**Четыре ноль пять**

Отчёт готов, и первая же строка в нём делает остальные почти лишними.

`Default: allow (incoming)`. Политика по умолчанию — разрешать. Всё, что не запрещено явно, открыто; тринадцать аккуратных правил `ALLOW` не добавляют ни одного закрытого порта.

Дальше — `3306` и `9200` наружу: те самые базы, которые ты нашёл в списке портов четыре дня назад и которые так и не закрыли. Правило `Anywhere ALLOW IN 185.14.29.0/24` — разрешить с этой подсети вообще всё. И `4444` с `33060`, которые слушают наружу и не упомянуты в правилах ни разу.

Алекс читает вывод по телефону и коротко ругается.

> — Подсеть моя. Ставил в июле для отладки, обещал убрать.
> — Политика по умолчанию тоже твоя?
> — Нет. Политика — с момента установки. Её просто никто не менял.

Ты разворачиваешь `default deny incoming`, потом заново открываешь нужное, проверяя после каждого шага, что SSH-сессия жива. Нагрузка не падает — атака идёт мимо этих портов, — но стена впервые за полгода начинает быть стеной.

> — Стена цела, — говорит Алекс. — Теперь надо остановить сам флуд. У Анны список: восемьсот сорок семь адресов. Руками мы их будем вносить до вечера.

```
┌──────────────────────────────────────────────────────────────┐
│                                                              │
│   Дыры найдены, лишнее закрыто. Но атака идёт ПРЯМО СЕЙЧАС.  │
│                                                              │
│   847 адресов ботнета. По одному — до вечера.                │
│   Нужны правила, сгенерированные из списка,                  │
│   и проверенные до того, как их применят.                    │
│                                                              │
│   Финал эпизода.                                             │
│                                                              │
└──────────────────────────────────────────────────────────────┘
```

**Дальше → [`s02e07`](../s02e07-block-botnet/) — блокировка ботнета: капстоун эпизода.**

Отчёт `fw_report.txt` вместе с `ports_report.txt` из s02e02 — полная карта поверхности `netshield` (см. [`PROJECTS.md`](../../docs/PROJECTS.md)): что слушает и что из этого доступно.

**Навык разблокирован:** правила фаервола — default-deny, чтение ufw status, опасные ALLOW.

---

## Ссылки и AI-usage

**Справка и книги:**

- `man ufw`, `man iptables`, `man nft`, `man iptables-save`
- Nemeth и др., «UNIX and Linux System Administration Handbook» — сетевая безопасность.
- Глубже — [`theory.md`](theory.md) и корневой [`RESOURCES.md`](../../docs/RESOURCES.md).

**Как работать с LILITH:**

Green:

- *«Объясни на примере разницу цепочек INPUT, OUTPUT и FORWARD.»*
- *«Почему `ufw enable` до `ufw allow 22` обрывает SSH и как этого избежать?»*
- *«Проверь мой вывод: почему строк ALLOW больше, чем закрытых портов?»*

Red:

- *«Заполни отчёт за меня.»* — разбор чужих правил руками стоит пройти один раз: он учит читать политику и колонку `From`, а не только номера портов.

---

---

---

---

## Навигация

- ◀ Назад: [s02e05 — Доверие](../s02e05-dns-spoofing-guard/)
- ▲ Сезон: [Season 2: Networking](../README.md) · [весь курс](../../README.md)
- ▶ Дальше: **[s02e07 — Восемьсот сорок семь](../s02e07-block-botnet/)**
