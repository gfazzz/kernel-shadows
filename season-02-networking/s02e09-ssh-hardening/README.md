# s02e09 — Закрыть и спрятать: «Финал Season 2»

```
Season 2: Networking               Серия s02e09 (Episode 08, капстоун сезона)
Локация: Цюрих 🇨🇭                  Дни 15–16 операции
Концепт: закалка sshd_config + туннели и шифрование канала
Тип: Type B — Configuration/Tools  Время: ~55 мин      Сложность: ⭐⭐⭐
```

> **Требования среды:** bash, без root — аудит читает конфигурацию из файла. Критерии — в [`mission.md`](mission.md).
> **Ранее:** в [`s02e08`](../s02e08-ssh-keys/) ты нашёл три ключа с правами `644`; команда перевыпустила ключи.

---

## I. Половина дела

К вечеру у всех новые ключи, правильные права, парольные фразы. Дмитрий уже собирается закрывать ноутбук, когда Алекс задаёт вопрос, который портит всем настроение.

> — А что изменилось на серверах?

Тишина.

> — Ничего. Мы поменяли то, чем заходим. А сервер по-прежнему принимает пароль от кого угодно и пускает root напрямую. Наши ключи сейчас — просто более удобный способ входа. Дверь как была открыта, так и осталась.

Он выводит на экран строку из журнала одного из серверов:

```
Failed password for root from 45.83.64.12 port 51044 ssh2
```

> — Это за последний час. Их таких четыре тысячи в сутки. Пока `PasswordAuthentication` включён, кто-то методично пробует, и однажды подберёт — не у нас, так у подрядчика с паролем `Server2024`.

Виктор смотрит на часы.

> — Сколько нужно, чтобы закрыть?
> — Четыре директивы в конфигурации. Плюс проверить, что мы не отрежем себя. И плюс второе — трафик между узлами всё ещё читаем, а Крылов его читает. Значит, туннели.

> **LILITH:** *«Закрыть и спрятать — две разные задачи. Первая убирает способы войти, вторая делает бессмысленным наблюдение. Обе решаются сегодня.»*

---

## II. Теория: конфигурация и каналы

### Что закрывают в `sshd_config`

```
PermitRootLogin no             # root — только через sudo от обычного пользователя
PasswordAuthentication no      # только ключи
PermitEmptyPasswords no
X11Forwarding no               # на сервере не нужен
MaxAuthTries 3
AllowGroups ssh-users          # явный список тех, кому можно
```

Каждая строка убирает конкретный способ войти. `PermitRootLogin no` лишает смысла перебор самой ценной учётной записи; `PasswordAuthentication no` обесценивает перебор паролей целиком — сервер просто не предложит такой метод.

**Закомментированная строка настройкой не является.** `#PermitRootLogin no` — это документация значения по умолчанию, а не запрет. Значение по умолчанию, к тому же, менялось между версиями OpenSSH. Аудит должен смотреть на активные строки, иначе конфигурация «выглядит закалённой», а поведение остаётся прежним.

Порядок применения:

```bash
sudo sshd -t                       # проверить синтаксис — обязательно
sudo systemctl reload ssh          # применить (systemd — Season 3)
```

Перед выключением паролей стоит убедиться, что ключ работает, **и держать вторую открытую сессию**: уже подключённые сессии не разрываются, и через них можно откатить ошибку.

### Как прячут трафик

SSH умеет пробрасывать порты внутри своего шифрованного соединения:

```bash
ssh -L 8080:localhost:80 user@server   # локальный порт → сервис на той стороне
ssh -R 9000:localhost:3000 user@server # порт на сервере → сервис на твоей машине
ssh -D 1080 user@server                # SOCKS-прокси: весь трафик приложения через сервер
```

Снаружи видно единственное SSH-соединение — не то, что внутри него.

VPN решает ту же задачу шире: шифрует **весь** трафик машины, а не отдельный канал. Современный выбор — WireGuard: пара ключей, короткий конфиг, шифр ChaCha20-Poly1305. Детали туннелей, VPN и остальных директив — в [`theory.md`](theory.md).

---

## III. Проверка: LILITH задаёт вопросы

**LILITH:** *«В конфигурации есть `PermitRootLogin no`, но строка закомментирована. Она работает?»*

<details>
<summary>Подумай 20 секунд</summary>

Нет. Символ `#` делает строку комментарием, а не настройкой — обычно так в поставке отмечают значение по умолчанию. Действовать будет именно умолчание, а оно менялось: в старых версиях OpenSSH это `yes`, в новых — `prohibit-password`.

Отсюда правило аудита: читать только активные строки. Конфигурация, где нужные значения присутствуют исключительно в комментариях, — самый неприятный случай, потому что выглядит она правильной.
</details>

**LILITH:** *«Фаервол настроен, вход по ключам. Зачем ещё шифровать канал?»*

<details>
<summary>Ответ</summary>

Потому что это три разные задачи. Фаервол решает, кого пускать. Ключи решают, кто именно вошёл. Ни то, ни другое не влияет на то, что видит наблюдатель на пути между узлами.

При перехвате трафика видно, кто с кем и когда соединяется, какие имена запрашивает, какого объёма данные передаёт, — и всё содержимое, если протокол не шифрован. Туннель или VPN оставляют наблюдателю только факт соединения.

Слои независимы и не заменяют друг друга: убери любой — исчезнет именно та защита, которую он давал.
</details>

---

## IV. Задание: капстоун сезона

Собери `sshd_harden_check.sh` — аудит конфигурации SSH-сервера по ключевым директивам:

```
=== Аудит закалки sshd_config: /etc/ssh/sshd_config ===
  ⚠️ PermitRootLogin = yes — root-логин должен быть запрещён
  ⚠️ PasswordAuthentication = yes — парольный вход выключить, оставить только ключи
  ⚠️ PermitEmptyPasswords = <не задано> — пустые пароли недопустимы
  ✓ X11Forwarding = no (ок)
---
Проблем: 3
```

```bash
cp starter/sshd_harden_check.sh artifacts/sshd_harden_check.sh
# заменить TODO (активное значение директивы → сравнение → счётчик)
bash artifacts/sshd_harden_check.sh /etc/ssh/sshd_config
bash tests/test.sh
```

ТЗ и критерии — в [`mission.md`](mission.md). Фикстуры теста воспроизводят реальные конфигурации: закомментированные значения рядом с активными, регистр директив вразнобой, отступы, значение `prohibit-password` (не `no`) и полностью закалённый вариант, на котором аудит обязан молчать.

<details>
<summary>Подсказка (если застрял на 5+ минут)</summary>

```bash
getval() {
    grep -iE "^[[:space:]]*$1[[:space:]]" "$cfg" \
        | grep -vE '^[[:space:]]*#' | tail -1 | awk '{print $2}'
}
v="$(getval PermitRootLogin)"
printf '%s' "$v" | grep -qiE '^no$' || issues=$((issues + 1))
```

Директивы `sshd_config` нечувствительны к регистру, поэтому `grep -i`. Отступ перед директивой допустим — отсюда `[[:space:]]*` в начале. Эталон — в `solution/`, открывай после попытки.
</details>

### Три ошибки, которые здесь делают

**Искать директиву обычным `grep` без якоря.** `grep PermitRootLogin` найдёт и закомментированную строку, и упоминание в комментарии абзацем выше. Нужен якорь начала строки, допуск на отступ и явное исключение комментариев.

**Считать «не задано» безопасным.** Отсутствие директивы означает значение по умолчанию, и оно не всегда безопасно и не всегда одинаково между версиями. В аудите отсутствие настройки — такой же повод для отметки, как и неверное значение: пусть явное будет явным.

**Принимать любое значение, кроме `yes`.** `PermitRootLogin prohibit-password` разрешает root-вход по ключу — это лучше, чем `yes`, но не то же самое, что `no`. Проверка вида «не равно yes» такую конфигурацию пропустит.

---

## V. Двадцать три ноль-ноль

Аудит проходит по конфигурациям всех тридцати четырёх серверов. Восемнадцать закалены, шестнадцать — нет; на четырёх `PermitRootLogin` стоит в `prohibit-password`, что не является ошибкой, но и не является тем, о чём договаривались.

Правки применяются по одной, с проверкой синтаксиса и второй открытой сессией. К девяти вечера пароли выключены везде.

К одиннадцати Дмитрий поднимает VPN-узел в Цюрихе, и все пятеро подключаются через него.

> **Анна:** — Он пробовал смотреть последние два часа. Видел поток шифротекста и ничего больше.
> **Виктор:** — Значит, теперь мы разговариваем.

Ты сидишь у окна с видом на мокрый Цюрих и считаешь: шестнадцать дней назад ты не знал, что такое TCP, и всерьёз думал, что «сервер лежит» — это диагноз, а не вопрос.

```
LILITH: SEASON 2 ЗАВЕРШЁН ✓
✓ адресация  ✓ порты и привязка  ✓ диагностика  ✓ DNS и подмена
✓ фаервол  ✓ массовая блокировка  ✓ ключи  ✓ закалка и шифрование

Проект netshield развёрнут: карта адресов, защита имён, фильтрация,
847 заблокированных адресов, доступ по ключам, закалённый sshd.

«Шестнадцать дней из шестидесяти. Противник эскалирует — и будет эскалировать
дальше. Season 3: System Administration. Санкт-Петербург, Таллин.
Там речь пойдёт о том, что происходит внутри машины.»
```

Сезонный проект `netshield` завершён (см. [`PROJECTS.md`](../../PROJECTS.md)); в Season 4 он ляжет под управление Ansible в составе `shadow_iac`.

**Дальше → Season 3 «System Administration» — пользователи, права, процессы и systemd.**

---

## Ссылки и AI-usage

**Справка и книги:**

- `man sshd_config`, `man ssh` (`-L`, `-R`, `-D`, `ProxyJump`), `man wg`, `man wg-quick`
- Рекомендации Mozilla по конфигурации OpenSSH — практичный чек-лист.
- Глубже — [`theory.md`](theory.md) и корневой [`RESOURCES.md`](../../RESOURCES.md).

**Как работать с LILITH:**

Green:

- *«Почему закомментированная директива `sshd_config` — не настройка?»*
- *«Разбери `-L`, `-R` и `-D` на конкретном сценарии — что куда ведёт.»*
- *«Ревью моего `sshd_harden_check.sh`: как он отличает активную строку от комментария?»*

Red:

- *«Напиши аудит за меня.»* — это финал сезона; ценность в том, чтобы увидеть, что он собирается из уже знакомых кусков.

---

```
Навигация:  ◀ s02e08 — SSH-ключи   │   Season 2 (финал)   │   Season 3 ▶
Season README: ../README.md    Курс: ../../README.md
```
