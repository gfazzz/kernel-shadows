# .dockerignore (СТАРТЕР)
#
# Пустой. Поэтому `COPY . /app` затягивает внутрь всё, что лежало
# в рабочем каталоге, — включая .git со всей историей и ключ,
# который никто не собирался класть в образ.
