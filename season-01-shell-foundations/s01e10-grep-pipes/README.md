# s01e10 — Океан логов: «Лупа по имени grep»

```
Season 1: Shell Foundations        Серия s01e10
Локация: Новосибирск, Академгородок 🇷🇺   День 5 операции, 03:52
Концепт: grep (фильтр строк) + конвейер (|)
Тип: Type C — Investigation        Время: ~50 мин      Сложность: ⭐⭐
```

> **Требования среды:** bash + grep, без root, без сети — разбираем журнал из [`data/`](../data/). Критерии — в [`mission.md`](mission.md).

---

## Previously

В [`s01e09`](../s01e09-logging-monitor/) ты собрал `server_monitor.sh` — мониторинг, который пишет журнал с метками времени.

---

## 03:52

Телефон вибрирует на подоконнике и падает на пол. Ты нащупываешь его в темноте — 03:52, незнакомый московский номер. Второй звонок за две минуты.

Женский голос, очень собранный, без «доброе утро».

> — Максим Соколов? Анна Ковалёва, форензика. Нас атаковали. DDoS на сервер Виктора, вчера в 03:47 — четыре с половиной тысячи запросов в секунду. Сервер выстоял.
> — Что от меня нужно?
> — Журнал доступа за сутки, четыре с лишним тысячи строк. Мне нужны цифры: сколько запросов было ненормальными, каких именно и когда был пик. Файл уже у тебя на почте.

Ты садишься за стол и открываешь `access.log`. Первый экран — сорок строк из четырёх с лишним тысяч:

```
185.220.101.47 - - [05/Oct/2025:03:47:23 +0000] "GET /admin HTTP/1.1" 403 0
185.220.101.47 - - [05/Oct/2025:03:47:23 +0000] "POST /login HTTP/1.1" 401 0
10.0.0.5      - - [05/Oct/2025:03:47:23 +0000] "GET /index.html HTTP/1.1" 200 512
```

Ты пролистываешь вниз и через минуту ловишь себя на том, что уже не читаешь — просто смотришь на бегущий текст. При такой скорости чтения журнал закончится к обеду, и в голове не останется ничего.

Приходит сообщение. Незнакомый контакт, подпись — Ольга Петрова, дата-сайентист.

```
Анна попросила подстраховать. Первое правило: не читай логи глазами.

Четыре тысячи строк — это не «много текста», это данные. К данным задают
вопрос, а не пролистывают их. Твой вопрос: какие запросы НЕ были нормальными.

Инструмент называется grep. Это лупа: она показывает только те строки,
где есть то, что ты ищешь. Остальные девяносто девять процентов
просто не попадут тебе на глаза.

— О.
```

## Чему ты научишься

- `grep` и тем флагам, которые действительно нужны; минимуму регулярных выражений, который окупается.
- Тому, что физически происходит в конвейере `|`.
- Почему счётчик `404` даёт 806, а не 807: привязка к кавычке меняет ответ.
- Метанавыку: **сначала вопрос, потом инструмент**. Иначе обвиняешь по ошибке.

Подробнее, с историей и подводными камнями, — в [`theory.md`](theory.md).

---

## Теория: фильтр и конвейер

### `grep` — оставить только нужные строки

`grep` читает поток строк и печатает те, что подходят под шаблон:

```bash
grep "ERROR" app.log          # строки, содержащие ERROR
grep -i "error" app.log       # без учёта регистра
grep -v " 200 " access.log    # ИНВЕРСИЯ: строки БЕЗ " 200 "
grep -c " 404 " access.log    # только количество
grep -n "ERROR" app.log       # с номерами строк
```

Флаг `-v` здесь ключевой. Вопрос «что было ненормальным» переворачивается в «всё, кроме нормального»: нормальный ответ веб-сервера — `200`, значит, интересны строки, где статуса `200` нет.

Шаблон — не просто подстрока, а регулярное выражение:

```bash
grep '^185\.'          access.log   # строки, НАЧИНАЮЩИЕСЯ с 185.
grep -E ' (401|403|404|5[0-9]{2}) ' access.log   # перечисление статусов
```

`^` — начало строки, `$` — конец, `[0-9]` — цифра, `.` — любой символ (поэтому в IP точку экранируют: `\.`). Вертикальная черта «или» и `+`/`{}` требуют флага `-E`. Подробнее — в [`theory.md`](theory.md).

### `|` — конвейер

Труба соединяет выход одной команды со входом следующей:

```bash
grep -v " 200 " access.log | wc -l      # отфильтровать → посчитать
grep " 404 " access.log | head -5       # найти → взять первые пять
```

Это и есть способ, которым Unix решает большие задачи: не одна программа, которая умеет всё, а десяток мелких, каждая из которых умеет ровно одно, и труба между ними.

> **Ольга:** *«Не ищи команду „проанализируй лог". Её нет и не будет. Есть „отфильтруй", „вырежи поле", „отсортируй", „посчитай одинаковые". Анализ — это то, как ты их составишь.»*

---

## Проверка: LILITH задаёт вопросы

**LILITH:** *«Нужны строки, где статус НЕ 200. Какой флаг?»*

<details>
<summary>Подумай 20 секунд</summary>

`grep -v " 200 "` — `-v` инвертирует условие и печатает строки, которые шаблону **не** соответствуют.

Пробелы вокруг числа не для красоты: без них шаблон совпадёт с `1200` в размере ответа, с адресом `10.0.200.5`, с датой. Фильтр по подстроке всегда ловит больше, чем задумано, — сузить его можно только контекстом вокруг.
</details>

**LILITH:** *«`grep -c` и `grep … | wc -l` дают одно число. Зачем труба?»*

<details>
<summary>Ответ</summary>

Затем, что `-c` умеет только считать свои же совпадения — это тупик. Труба соединяет что угодно с чем угодно:

```bash
grep -v " 200 " access.log | awk '{print $1}' | sort | uniq -c | sort -rn | head
```

Здесь фильтр, извлечение поля, сортировка и подсчёт — четыре разные программы, ни одна из которых не знает про остальные. Так из лога получается рейтинг атакующих. `-c` — частный случай; конвейер — принцип.
</details>

---

## Задание: отделить подозрительное

Это серия-**расследование**. Инструмент писать не нужно: нужно ответить на вопросы о конкретной ночи, и каждый ответ добывается одной командой.

Объект разбора — настоящий журнал из [`data/access.log`](../data/access.log): 4400 строк за сутки, включая ту самую атаку. Артефакт — **заполненный отчёт** `artifacts/attack_report.txt`:

```
total_requests=4400
requests_200=2694
suspicious_requests=1706
count_403=238
sqli_attempts=29
attack_minute_requests=221
```

```bash
cp starter/attack_report.txt artifacts/attack_report.txt
# заполнять, добывая каждое число grep-ом и конвейером
bash tests/test.sh
```

Вопросы и критерии — в [`mission.md`](mission.md). Тест не хранит правильных ответов: он считает их по тому же журналу, что и ты.

<details>
<summary>Подсказка (если застрял на 5+ минут)</summary>

```bash
L=../data/access.log
wc -l < "$L"                    # всего строк
grep -c '" 200 ' "$L"           # успешных
grep -vc '" 200 ' "$L"          # всех остальных
grep -c 'UNION SELECT' "$L"     # попыток инъекции
```

Обрати внимание на кавычку в шаблоне `'" 404 '` — она привязывает поиск к статусу и отсекает совпадения с размером ответа. Эталон — в `solution/`, открывай после попытки.
</details>

---

## Разбор типичных ошибок

**Считать статус по голому числу.** `grep -c ' 404 '` даёт 807, а верный ответ — 806. Лишняя строка — запрос, у которого **размер ответа** оказался равен 404. То же с 500: 63 против 62. Ошибка на единицу выглядит безобидно, пока такой отчёт не приложат к делу. Привязка к закрывающей кавычке запроса (`'" 404 '`) убирает обе.

**Проверять только то, что ожидал увидеть.** Статусы `401` и `403` в сумме дают 638 запросов — это не «фон», это чья-то методичная работа. Отчёт, где посчитаны только `404` и `500`, потому что они «про ошибки», пропускает главное.

**Забыть про пустой результат.** Если совпадений нет, `grep` возвращает код `1` — это не ошибка, а ответ «ничего». В конвейере с `set -e` такой ответ обрывает всю цепочку, и вместо нуля получается тишина. Ноль — тоже находка, и его надо записать.

---

## Debriefing

**Одиннадцать чисел**

Три команды и полторы минуты — и океан превращается в одиннадцать чисел.

Тысяча семьсот шесть запросов из четырёх с половиной тысяч не вернули `200`. Четыреста ответов `401` — кто-то подбирал вход. Двести тридцать восемь `403`, из них двадцать девять с `UNION SELECT` в строке запроса. Четыреста пятьдесят четыре запроса пришли от инструмента, который в user-agent честно назвался сканером. И двести двадцать один запрос уложился в одну минуту — 03:47.

Ты отправляешь результат Анне. Ответ приходит в 04:31 — она тоже не спит.

```
Вижу. Это не DDoS ради нагрузки, это разведка под прикрытием шума.
Кто-то искал вход, пока все смотрели на график трафика.

Мне нужны АДРЕСА и частота. Не строки — цифры.
Кто стучался чаще всех.

— А.К.
```

Ты смотришь на свой вывод и понимаешь, в чём проблема. Строки есть. Адреса в них есть — первым полем. Но чтобы получить «кто чаще всех», надо вырезать это поле из каждой строки и посчитать повторы. `grep` так не умеет: он работает со строкой целиком, для него строка — неделимая.

```
┌──────────────────────────────────────────────────────────────┐
│                                                              │
│   Фильтровать научился. Из тысяч строк — сотни нужных.       │
│                                                              │
│   Но Анне нужны не строки, а АДРЕСА и ЧАСТОТА.               │
│   Строку надо разложить на поля и посчитать одинаковые.      │
│                                                              │
│   Для grep строка — единое целое. Нужен инструмент,          │
│   который видит в ней колонки.                               │
│                                                              │
└──────────────────────────────────────────────────────────────┘
```

04:35. За окном ещё темно, и спать уже бессмысленно.

**Дальше → [`s01e11`](../s01e11-awk-stats/) — `awk`, `sort`, `uniq`: кто атакует чаще всех.**

**Навык разблокирован:** grep (фильтр строк) + конвейер (|).

---

## Ссылки и AI-usage

**Справка и книги:**

- `man grep`, `man 7 regex`
- Shotts, «The Linux Command Line», гл. 19 «Regular Expressions», гл. 20 «Text Processing».
- Глубже — [`theory.md`](theory.md) и корневой [`RESOURCES.md`](../../docs/RESOURCES.md).

**Как работать с LILITH:**

Green:

- *«Почему `grep 'a|b'` без `-E` ничего не находит, а `grep -E 'a|b'` находит?»*
- *«Как одним конвейером посчитать, сколько раз встречается каждый HTTP-статус?»*
- *«Почему `grep -c ' 404 '` и `grep -c '" 404 '` дают разные числа на этом журнале?»*

Red:

- *«Посчитай всё за меня.»* — смысл серии в том, чтобы самому увидеть, как безобидный шаблон даёт число, отличающееся на единицу.

---

---

---

## Навигация

- ◀ Назад: [s01e09 — Память системы](../s01e09-logging-monitor/)
- ▲ Сезон: [Season 1: Shell & Foundations](../README.md) · [весь курс](../../README.md)
- ▶ Дальше: **[s01e11 — Колонки](../s01e11-awk-stats/)**
