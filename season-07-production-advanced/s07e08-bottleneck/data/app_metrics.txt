# Метрики приложения за то же окно наблюдения — те самые, что приведены в
# порядок в s07e05. Величины уже сведены к перцентилям и округлены до
# миллисекунд: сравнивать доли секунды с плавающей точкой между машинами
# нельзя (см. §4.3 плана).
#
# key                       value   пояснение
request_p50_ms                 96   половина запросов быстрее
request_p95_ms                620   95-й перцентиль полного времени ответа
pool_wait_p50_ms               31   ожидание соединения к базе, медиана
pool_wait_p95_ms              410   оно же, 95-й перцентиль
db_query_p95_ms               104   собственно запрос к базе, без ожидания
render_p95_ms                  84   формирование ответа

# Пул соединений к базе
pool_max                       32   AURORA_MAX_INFLIGHT из ConfigMap (s07e03)
pool_in_use_max                32   наибольшее число занятых за окно
pool_waiters_max               47   наибольшая длина очереди ожидающих

# Нагрузка
requests_per_second           340
replicas                        5
