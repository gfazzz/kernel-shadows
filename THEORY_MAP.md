# THEORY_MAP — карта зависимостей концептов

**Назначение.** Единая карта «какой концепт где вводится» — чтобы ловить
**зависимости «вперёд»** (forward-deps): ситуации, когда инструмент/концепт
используется в зачётном коде раньше, чем объясняется. Это прямое требование
плана v2.0 (§8): «Ввести `THEORY_MAP.md` — чтобы ловить forward-deps как T1–T4».

**Правило.** Концепт X можно использовать в серии N только если X введён в серии ≤ N.
Исключение — **осознанный «предпросмотр»**: тогда в README серии ставится явная пометка
«предпросмотр, разберём в sNNeNN», и X **не попадает в зачётный код** (тест не должен
требовать неизученного).

> Нумерация ниже — текущая (`epNN`); после Этапа 1 (миграция на `sNNeNN`) колонка
> «серия» обновляется на посезонные идентификаторы. Соответствие — в релизе v2.0.

---

## 1. Подтверждённые forward-deps (долги v2.0)

| # | Концепт | Вводится | Используется раньше | Приоритет | Действие v2.0 |
|---|---------|----------|---------------------|-----------|---------------|
| ~~**T1**~~ ✅ | `awk` | s01e11 (Text Processing) | ~~ep02~~ | Средний | **ЗАКРЫТО (v2.0).** В пересобранном ep02 (s01e07-07) `awk` не используется — имя хоста берётся через `${line%% *}`; `awk` вводится по месту в s01e11. |
| ~~**T2**~~ ✅ (S2) | `systemd` / `systemctl` | ep10 (S3) — Processes & Systemd | ~~ep06/ep07 (S2)~~ | **Высокий** | **ЗАКРЫТО для S2 (v2.0).** В пересобранных s02e04-07 `systemctl restart` НЕ в зачётном коде; `systemd-resolved` только read-only (`resolvectl query`), с пометкой «systemd — Season 3». Полное введение systemd остаётся в S3 (ep10). |
| ~~**T3**~~ ✅ | Docker | ep14 (S4) — Docker Basics | ~~ep04 (S1)~~ | **Высокий** | **ЗАКРЫТО для S1 (v2.0).** В пересобранном ep04 (s01e13-12) `docker-ce` не ставится заранее; в README/theory явно: Docker — в Season 4. (Проверить также, что S4 не тянет Docker раньше ep14.) |
| ~~**T4**~~ ✅ | `fail2ban` | **s05e04** — от журнала к блокировке | ~~ep07 (S2)~~ | Средний | **ЗАКРЫТО (v2.0, полностью 2026-08-08).** В s02e07 `fail2ban` был advanced-превью (не в зачётном коде); блокировка делалась генерацией правил `ufw`. Полноценно введён в `s05e04` (Type B, `jail.local`, 12/12): три числа (maxretry/findtime/bantime), ловушки `ignoreip` (пусто → банишь себя; `0.0.0.0/0` → не банишь никого), джейлы sshd/nginx/recidive, слепая зона неверного `logpath`. Метанавык: защита назначает цену, а не запрещает. |
| ~~**T5**~~ ✅ | **текстовый редактор** (`nano`/`vim`) | **s01e05** — Правка на месте | ~~s01e01+ и все `Type B`-серии~~ | **Высокий** | **ЗАКРЫТО (v2.0).** Найдено при ревизии: студент писал `.sh` с первой серии и правил конфиги во всех `Type B`, но редактор не преподавался **нигде** — ни серии, ни строки в CURRICULUM. Введена новая серия (со сдвигом остальных): `nano`, режимы `vim`, минимум для выживания в `vi`, копия перед правкой. Стоит до первого написания скрипта. |
| ~~**T6**~~ ✅ | **файловые операции** (`mkdir`, `cp`, `mv`, `rm`, `touch`) и **`man`** | **s01e04** — Рабочее место | ~~s01e01 (`cp starter/… artifacts/…`) и далее везде~~ | **Высокий** | **ЗАКРЫТО (v2.0).** Найдено `tools/check_tools.sh` после указания автора считать инструменты такими же forward-deps, как концепты: `cp` требовался от студента в первом же задании, `mkdir`/`touch`/`mv` не встречались в S1 вообще, а «см. `man X`» стояло в каждой серии без объяснения, как читать справку. Введена новая серия: пять команд, разделы `man`, `man -k`, необратимость `rm`, принцип «на чужом — только копии». |

| ~~**T7**~~ ✅ | **`tar`** | **s03e11** — стратегии бэкапа | ~~s04e04 (`docker save \| tar -x`)~~ | Средний | **ЗАКРЫТО (2026-08-07).** Найдено `tools/check_tools.sh` при вводе s04e04: `tar` встречался в тексте курса, но не преподавался нигде (`NONE`). В зачётном коде его не было ни разу, поэтому достаточно было ввести — раздел добавлен в теорию s03e11 (создать, **посмотреть до распаковки**, распаковать, передать по конвейеру; чего `tar` не делает и почему для регулярных копий нужен `rsync`). `check_tools.sh` обновлён: `tar` → s03e11. |

| ~~**T8**~~ ✅ | **`curl`** | **s02e03** — диагностика сети | ~~s04e07 (`curl -fsS …/healthz` в выкате)~~ | Средний | **ЗАКРЫТО (2026-08-08).** Найдено `tools/check_tools.sh` при вводе s04e07. `curl` стоял последней ступенью лестницы диагностики в теории s02e03 и в трёх других теориях S2, но в README/mission не появлялся ни разу — то есть студент его не встречал. Раздел добавлен в README s02e03 («отвечает ли приложение»), с ключом `-f` как главным пунктом: без него код возврата ноль даже на 500, и именно это ломает проверки готовности в конвейерах. `check_tools.sh` обновлён: `curl` → s02e03. |

| ~~**T9**~~ ✅ | **`python3`, `gcc`, `make`** | **s06e10** (Python), **s06e01** (`gcc` и кросс-компиляция), **s06e11** (`make`/kbuild) | ~~s05e09 (`python3 -c` для декода hex)~~ | Средний | **ЗАКРЫТО (2026-08-21).** Найдено `tools/check_tools.sh` при вводе Type D в S6: с появлением серий, где сдаётся программа, языки и сборка стали инструментами курса и подпали под то же правило. В зачётном коде до S6 их не было ни разу; единственное раннее упоминание — однострочник `python3 -c` в теории s05e09. Он заменён на `printf` с шестнадцатеричными аргументами (делает то же самое и не требует Python), а сам однострочник убран под `<details>` с явной пометкой предпросмотра. `check_tools.sh` дополнен тремя строками: `python3` → s06e10, `gcc` → s06e01, `make` → s06e11. |

**Правило, из которого выросли T5–T9:** forward-dep бывает не только по концепту, но и по
**инструменту** — редактору, команде, утилите. Перед вводом серии прогонять
`bash tools/check_tools.sh`: он находит первое упоминание инструмента и сверяет с точкой
его ввода. Не преподаётся вовсе (`NONE`) или используется раньше — либо вводим, либо
помечаем «предпросмотр, разберём в sNNeNN», либо убираем из зачётного кода.
Остаточные предпросмотры S1: `grep`/`wc`/`sort` в подсказках s01e02 (вводятся в s01e10–11)
и `chmod` в диагностике s01e01 (права — Season 3) — оба помечены в тексте явно.

---

## 2. Опорная прогрессия концептов (S1 → S8)

Базовый порядок аудитом признан верным и кумулятивным по темам. Карта — ориентир
для дробления серий и проверки, что новый концепт не «забегает вперёд».

| Серия (тек.) | Ключевые вводимые концепты | Опирается на |
|--------------|----------------------------|--------------|
| ep01 (S1) | терминал, файловая система, навигация, pipes, права (базово) | — |
| ep02 (S1) | shell-скрипты: переменные, условия, циклы, функции | ep01 |
| ep03 (S1) | text processing: `grep`, `sed`, **`awk`**, regex | ep01–02 |
| ep04 (S1) | пакеты: `apt`, репозитории, сервисы (базово) | ep01–03 |
| ep05 (S2) | TCP/IP, сокеты, `ip`, `ss`, маршрутизация | ep01–04 |
| ep06 (S2) | DNS, `dig`, resolver | ep05 |
| ep07 (S2) | firewall: `iptables`/`ufw` | ep05–06 |
| ep08 (S2) | VPN, SSH-туннели, `ssh`/`scp` | ep05–07 |
| ep09 (S3) | пользователи, права, `sudo`, `sudoers` | ep01–04 |
| ep10 (S3) | **`systemd`**, юниты, `systemctl`, journald | ep09 |
| ep11 (S3) | диски, разделы, LVM, файловые системы | ep09–10 |
| ep12 (S3) | бэкапы, восстановление, `rsync`, снапшоты | ep09–11 |
| ep13 (S4) | Git, ветки, слияния, версионирование | ep02 |
| ep14 (S4) | **Docker**, образы, контейнеры, Compose | ep04, ep13 |
| ep15 (S4) | CI/CD, пайплайны, автосборка образов | ep13–14 |
| ep16 (S4) | Ansible, IaC, плейбуки | ep09–15 |
| ep17 (S5) | аудит безопасности, **`fail2ban`**, harden-инструменты | ep07–10 |
| ep18 (S5) | пентест-инструментарий | ep05–17 |
| ep19 (S5) | incident response, форензика (базово) | ep10, ep17 |
| ep20 (S5) | hardening: sysctl, SSH-hardening, ASLR | ep07, ep10, ep17 |
| ep21 (S6) | Raspberry Pi, GPIO (мок через `gpio-mockup`) | ep01–04 |
| ep22 (S6) | дроны, Python-автоматика, SITL | ep21 |
| ep23 (S6) | IoT, MQTT (publish/subscribe) | ep05–06, ep21 |
| ep24 (S6) | kernel modules, `.ko`, драйверы | ep02, ep21 |
| ep25 (S7) | Kubernetes, оркестрация | ep14–16 |
| ep26 (S7) | мониторинг, observability | ep10, ep25 |
| ep27 (S7) | performance tuning | ep10–11, ep25 |
| ep28 (S7) | security hardening (production) | ep17, ep20, ep25 |
| ep29 (S8) | DDoS/zero-day/APT под нагрузкой | S1–S7 |
| ep30 (S8) | форензика, supply-chain, hardening | S3, S5, S7 |
| ep31 (S8) | offensive ops, botnet cleanup (Ansible) | S4, S5 |
| ep32 (S8) | финальная интеграция (капстоун) | S1–S7 |

---

## 3. Дубли справочников (T6) — свести в единый источник

Повторяющиеся справочные блоки должны жить в одном месте (`RESOURCES.md`
или общий `theory.md`-раздел), а серии — **ссылаться**, а не копировать.

| Справочник | Где дублируется (тек.) | Каноничный дом |
|------------|------------------------|----------------|
| sysctl-hardening (`tcp_syncookies`, ASLR…) | ≥6 мест: ep20, ep27, ep28×3, ep29, ep07 | `RESOURCES.md` → раздел hardening |
| SSH-hardening (`sshd_config`) | ep17, ep20, ep21, ep28 | `RESOURCES.md` → раздел SSH |
| cheatsheet-файлы | перекрывают hardening-эпизоды | ссылка из серии |

---

*Обновлять при каждом добавлении/переносе концепта. Перед вводом концепта в серию —
проверить по этой карте, что он не используется раньше точки ввода.*
