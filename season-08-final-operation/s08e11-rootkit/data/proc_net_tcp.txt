# /proc/net/tcp, состояние 0A (LISTEN), порт переведён из hex в десятичный
# независимым разборщиком. Ядро не умеет прятать отсюда строки так же
# легко, как из вывода ss.
#
# proto local_port
tcp 22
tcp 80
tcp 443
tcp 9100
tcp 31337
