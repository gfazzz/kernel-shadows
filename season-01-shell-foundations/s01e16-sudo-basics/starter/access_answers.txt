# Что покажет московский сервер — s01e16 (ШАБЛОН)
#
# Заполни значения справа от «=». Пробелы вокруг «=» не важны.
# Строки, начинающиеся с #, — комментарии.
#
# Объект разведки: ../data/moscow_server_access.txt (четыре команды в разделах)
#
# Проверка:  bash tests/test.sh

# --- Кто я на этой машине ----------------------------------------------------
# имя пользователя
whoami=
# числовой идентификатор пользователя (UID)
uid=
# сколько групп у меня всего (основная плюс дополнительные)
groups_total=
# группа, через которую я получу доступ к журналам операции
ops_group=

# --- Что я смогу прочитать в /srv/ops ----------------------------------------
# файл, который читают все, включая посторонних
world_readable=
# файл, который я прочитаю ТОЛЬКО потому, что состою в группе adm
readable_via_group=
# файл, который я не прочитаю никак: он чужой и закрыт
unreadable=
# каталог, куда я могу войти и записать что угодно
world_writable_dir=

# --- Права ------------------------------------------------------------------
# права collect.sh цифрами
collect_octal=
# права plan.txt цифрами
plan_octal=
# файл с битом SUID (буква s вместо x у владельца)
suid_file=

# --- sudo -------------------------------------------------------------------
# сколько команд мне разрешено выполнять через sudo
sudo_allowed_count=
# от чьего имени они выполняются
sudo_runas=
# сколько раз я успешно воспользовался sudo 8 октября
sudo_used=
# команда, которую sudo мне ЗАПРЕТИЛ
sudo_denied=
