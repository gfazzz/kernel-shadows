# РАЗВЕДДАННЫЕ — s01e03
# Источники: три файла на «сервере» (season-01-shell-foundations/data/test_environment)
#
# Дойди до каждого файла и прочитай его. Значения выпиши точно как в источнике,
# без кавычек и лишних пробелов. Строки на # игнорируются.

# --- из documents/briefing.txt ---
# Уровень допуска оператора (строка CLEARANCE, только номер уровня: например 1)
clearance_level=

# --- из documents/.secret_location ---
# Широта точки встречи (как записана в файле, например 55.7558°N)
latitude=
# Долгота точки встречи
longitude=
# Время встречи (только часы:минуты, например 14:00)
meeting_time=

# --- из .next_server (в корне «сервера») ---
# IP следующего узла
next_ip=
# Порт SSH
next_port=
# Имя пользователя
next_user=
