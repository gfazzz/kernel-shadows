# Настройка сбора — фрагмент prometheus.yml, действующий в verne-prod.
global:
  scrape_interval: 15s
  scrape_timeout: 10s
  evaluation_interval: 30s

# Период сбора — 15 секунд. Отсюда нижняя граница окна для rate():
# в окне должно уместиться минимум ЧЕТЫРЕ точки, иначе одна пропущенная
# выборка (перезапуск, таймаут, перегрузка) оставляет окно с одной точкой,
# и rate() не возвращает ничего. Пустой результат в правиле оповещения
# означает «условие не выполнено» — то есть тишину в момент аварии.
#
#   минимум    4 * 15s = 1m
#   разумно    5m — переживает четыре подряд пропущенных выборки
