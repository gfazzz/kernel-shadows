# mqtt_report.txt — разбор обмена в сети shadow_mesh (СТАРТЕР)
#
# Источник — data/mqtt_dump.txt: журнал брокера mosquitto, снятый с
# ключом -v. Каждый пакет сведён к одной строке. Шапка файла (строки с #)
# содержит сведения о самом брокере — их тоже надо прочитать.
#
# Формат: ключ=значение, без пробелов вокруг «=». Строки с # — комментарии.
# Правь копию в artifacts/, потом проверяй:  bash tests/test.sh

# ─────────────────────────────────────────────────────────────────
# 1. Что вообще происходило
# ─────────────────────────────────────────────────────────────────

# Сколько пакетов брокер ПРИНЯЛ (строки «Received …»).
total_received=

# Сколько разных клиентов присутствует в журнале.
clients=

# Сколько разных тем (topic) фигурирует в публикациях.
unique_topics=

# Сколько публикаций (PUBLISH) принял брокер.
publish_count=

# Сколько подписок (SUBSCRIBE) он принял.
subscribe_count=

# ─────────────────────────────────────────────────────────────────
# 2. Кто это слушает
# ─────────────────────────────────────────────────────────────────

# Сколько подписок содержат шаблон (+ или #).
wildcard_subs=

# Клиент, подписанный на «#» — то есть на ВСЁ, что проходит через
# брокер. Имя клиента целиком.
rogue_client=

# ─────────────────────────────────────────────────────────────────
# 3. Как доставляется
# ─────────────────────────────────────────────────────────────────

# Публикаций с QoS 0 (отправил и забыл).
qos0_publishes=

# Публикаций с QoS 1 (доставить хотя бы раз).
qos1_publishes=

# Публикаций с QoS 2 (доставить ровно раз).
qos2_publishes=

# QoS, с которым публикуются КОМАНДЫ (темы с /cmd/).
cmd_qos=

# Сколько публикаций помечено флагом retained.
retained_count=

# ─────────────────────────────────────────────────────────────────
# 4. Ловушки
# ─────────────────────────────────────────────────────────────────

# Клиент, чьё соединение брокер закрыл по ошибке сокета.
offline_node=

# Тема, в которой от этого клиента осталось retained-сообщение.
stale_retained_topic=

# Значение, которое брокер будет отдавать в этой теме всем новым
# подписчикам — уже после того, как узел пропал.
stale_retained_value=

# Клиент, чей идентификатор занят дважды: брокер закрывает старое
# соединение при подключении нового с тем же именем.
dup_client_id=

# Сколько раз это произошло.
dup_disconnects=

# Сколько клиентов объявили «последнее слово» (will) при подключении.
lwt_clients=

# ─────────────────────────────────────────────────────────────────
# 5. Что видно постороннему
# ─────────────────────────────────────────────────────────────────

# Порт, на котором слушает брокер (из шапки журнала).
listener_port=

# Включён ли TLS: yes или no (из шапки журнала).
tls_enabled=

# Пароль, которым подключаются узлы. То, что его вообще можно сюда
# выписать, и есть находка.
plaintext_password=
