# s02e04 — Имена: «Кто отвечает на вопрос „где"»

```
Season 2: Networking               Серия s02e04 (Episode 06, ч. 1)
Локация: Стокгольм 🇸🇪, Bahnhof Pionen   День 10 операции, 11:00
Концепт: разрешение имён — dig, типы записей, порядок резолва
Тип: Type B — Configuration/Tools  Время: ~50 мин      Сложность: ⭐⭐
```

> **Требования среды:** bash, без root, без сети — тест подменяет `dig`. Критерии — в [`mission.md`](mission.md).
> **Ранее:** [`s02e03`](../s02e03-net-diagnostics/) — `net_diag.sh` и baseline сети. Episode 05 закрыт.

---

## I. Тридцать метров под скалой

Дата-центр Bahnhof Pionen построен в бывшем противоатомном бункере под гранитом Стокгольма. Внутри — водопад, оранжерея и серверные стойки, подсвеченные так, будто кто-то экранизировал киберпанк и забыл разобрать декорации.

Встречает Эрик Йоханссон — инженер, который здесь работает восемь лет и относится к обстановке буднично.

> — Красиво, да. Первые две недели все фотографируют. Потом привыкаешь и видишь только показания.

На его мониторе — панель мониторинга:

```
⚠ DNS Resolution Failures:      47%
⚠ Cache Poisoning Attempts:     23 detected
```

> — Резолв ломается почти на половине запросов. И кто-то настойчиво пытается подсунуть нашим резолверам чужие ответы.
> — Зачем? Проще же атаковать сам сервер.
> — Незачем атаковать сервер, если можно изменить ответ на вопрос «где этот сервер». Ты подключишься куда скажут, сам, добровольно, с правильным именем в адресной строке.

> **LILITH:** *«Люди помнят имена, машины работают с адресами. DNS — перевод между ними, и это единственный слой, где ты доверяешь чужому ответу до того, как что-либо проверил. Прежде чем защищать — научись читать.»*

---

## II. Теория: как имя превращается в адрес

### Иерархия и путь запроса

Доменное имя читается справа налево: `mail.example.com.` — это корень (точка в конце), зона `com`, зона `example`, узел `mail`. Каждый уровень знает, кто отвечает за уровень ниже.

Резолвер, не имея ответа в кеше, спрашивает корневые серверы, те отсылают к серверам `com`, те — к серверам `example.com`, и уже последние дают ответ. Это видно целиком:

```bash
dig +trace example.com
```

### `dig` — основной инструмент

```bash
dig example.com A +short      # только значения
dig example.com MX +short     # почтовые серверы с приоритетами
dig example.com               # полный ответ: секции, TTL, какой сервер ответил
dig @8.8.8.8 example.com      # спросить конкретный резолвер
dig -x 8.8.8.8                # обратный запрос: адрес → имя
```

| Тип | Что хранит |
|---|---|
| `A` / `AAAA` | адрес IPv4 / IPv6 |
| `MX` | почтовые серверы с приоритетом |
| `NS` | серверы имён, отвечающие за зону |
| `CNAME` | алиас: «это имя на самом деле то имя» |
| `TXT` | произвольный текст: SPF, DKIM, подтверждения владения |
| `PTR` | обратная запись: адрес → имя |

**Приоритет MX устроен наоборот интуиции**: `5 mail-b` важнее, чем `10 mail-a`. Меньшее число — выше приоритет, и отправитель пробует сначала его.

### Порядок резолва на самой машине

Прежде чем спросить DNS, система заглядывает в локальные источники. Порядок задан в `/etc/nsswitch.conf`:

```
hosts: files dns
```

`files` — это `/etc/hosts`, и он идёт **первым**. Одна строка в этом файле переопределяет любой ответ DNS, и `dig` этого не покажет: он спрашивает DNS напрямую, минуя системный резолвер.

```bash
cat /etc/hosts                # локальные переопределения
cat /etc/resolv.conf          # какие резолверы использует система
resolvectl query example.com  # запрос через системный резолвер (Ubuntu)
```

Разница между `dig example.com` и `getent hosts example.com` — ровно в этом: первый спрашивает DNS, второй проходит весь путь, каким пойдёт настоящая программа.

> **Эрик:** *«Первое место, куда я смотрю на подозрительной машине, — не логи и не сеть. `/etc/hosts`. Это две секунды, и это самый дешёвый способ перенаправить трафик, который знают все.»*

---

## III. Проверка: LILITH задаёт вопросы

**LILITH:** *«MX вернул `10 mail-a` и `5 mail-b`. Какой сервер попробует отправитель первым?»*

<details>
<summary>Подумай 20 секунд</summary>

`mail-b`. У записей MX меньшее число означает более высокий приоритет: `5` — основной сервер, `10` — резервный.

Перепутать легко, и последствия тихие: почта продолжит доставляться, но через резервный узел — с задержками, другими лимитами и, возможно, мимо ожидаемых политик фильтрации.
</details>

**LILITH:** *«Домен резолвится в незнакомый адрес, но `dig` показывает правильный. Где ещё мог вмешаться атакующий?»*

<details>
<summary>Ответ</summary>

В `/etc/hosts`. Он имеет приоритет над DNS согласно `nsswitch.conf`, а `dig` его вообще не читает — отсюда и расхождение между «инструмент показывает правильно» и «браузер идёт не туда».

Проверять надо тем, что проходит настоящий путь: `getent hosts домен`. Если его ответ отличается от `dig +short`, разница объясняется локальными файлами — и это уже повод для разговора.
</details>

---

## IV. Задание: научиться спрашивать

Собери `dns_lookup.sh` — делает запрос через `dig` и показывает результат с количеством записей:

```
$ ./dns_lookup.sh google.com MX
=== google.com [MX] ===
5 gmail-smtp-in.l.google.com.
10 alt1.gmail-smtp-in.l.google.com.
Записей: 2
```

Отсутствие записи — сообщение и ненулевой код возврата: пустой ответ не должен выглядеть как успешный.

```bash
cp starter/dns_lookup.sh artifacts/dns_lookup.sh
# заменить TODO (dig … +short → проверка пустого → вывод)
bash artifacts/dns_lookup.sh example.com A
bash tests/test.sh
```

ТЗ и критерии — в [`mission.md`](mission.md). Тест подменяет `dig` заглушкой с фиксированными ответами: домен с одной записью, домен с двумя, домен с несуществующим типом и домен, которого нет.

<details>
<summary>Подсказка (если застрял на 5+ минут)</summary>

```bash
ans="$(dig "$domain" "$type" +short 2>/dev/null)"
[ -z "$ans" ] && { echo "$domain [$type]: запись не найдена"; exit 1; }
printf '%s\n' "$ans"
echo "Записей: $(printf '%s\n' "$ans" | grep -c .)"
```

`grep -c .` считает непустые строки — на пустом вводе даст ноль, а не единицу, в отличие от `wc -l` с одной пустой строкой. Эталон — в `solution/`, открывай после попытки.
</details>

### Три ошибки, которые здесь делают

**Считать пустой ответ успехом.** `dig` завершается кодом `0` и при отсутствии записи — с его точки зрения запрос прошёл нормально, просто ответ пуст. Скрипт, который на это не смотрит, отчитается об успехе и передаст дальше пустую строку; ошибка всплывёт через два шага, в месте, где её причина уже не видна.

**Считать записи через `wc -l` без проверки.** Пустая строка в переменной даёт `wc -l` единицу — получится «Записей: 1» при нуле записей. `grep -c .` считает именно непустые.

**Проверять адрес только через `dig`.** `dig` спрашивает DNS. Программы идут через системный резолвер, который сначала читает `/etc/hosts`. Аудит, использующий только `dig`, слеп ровно к самой дешёвой подмене — и именно поэтому её и делают.

---

## V. Сорок семь процентов

Ты прогоняешь запросы по списку доменов операции. Часть отвечает мгновенно, часть — со второго раза, а три домена дают разные адреса при повторных запросах с интервалом в минуту.

Эрик смотрит на вывод и хмурится.

> — Вот это уже интересно. Балансировка так не выглядит: адреса из разных стран, TTL короткий, и один из них вообще не наш.

Он открывает соседнее окно, где идёт захват трафика.

> — Кто-то подсовывает резолверу ответы быстрее, чем это делает настоящий сервер зоны. Классическое отравление кеша: успел ответить первым — твой ответ и запомнят.
> — Как это ловить?
> — Сравнением. Ответ, полученный обычным путём, сверяют с ответом от доверенного источника и с известным эталоном. Расхождение — сигнал.

```
┌──────────────────────────────────────────────────────────────┐
│                                                              │
│   Ты умеешь спрашивать. Но `dig` вернул ОТВЕТ —              │
│   а он правдивый?                                            │
│                                                              │
│   Кто-то травит кеш: подменяет ответы, и знакомое имя        │
│   резолвится в чужой сервер. Ты видишь нормальный адрес,     │
│   а трафик уходит не туда.                                   │
│                                                              │
│   Следующий шаг — научиться ловить подмену.                  │
│                                                              │
└──────────────────────────────────────────────────────────────┘
```

**Дальше → [`s02e05`](../s02e05-dns-spoofing-guard/) — обнаружение подмены DNS: капстоун эпизода.**

`dns_lookup.sh` — DNS-модуль `netshield` (см. [`PROJECTS.md`](../../PROJECTS.md)).

---

## Ссылки и AI-usage

**Справка и книги:**

- `man dig`, `man host`, `man getent`, `man hosts`, `man resolv.conf`, `man nsswitch.conf`
- **RFC 1034 / 1035** — основы DNS; **RFC 7766** — DNS поверх TCP.
- Nemeth и др., «UNIX and Linux System Administration Handbook» — глава о DNS.
- Глубже — [`theory.md`](theory.md) и корневой [`RESOURCES.md`](../../RESOURCES.md).

**Как работать с LILITH:**

Green:

- *«Почему у MX меньшее число означает более высокий приоритет?»*
- *«В чём разница между `dig example.com` и `getent hosts example.com`?»*
- *«Ревью моего `dns_lookup.sh`: как он ведёт себя, если `dig` вернул пустой ответ?»*

Red:

- *«Напиши `dns_lookup.sh` за меня.»* — здесь важно самому увидеть, что пустой ответ и ошибка — разные вещи.

---

```
Навигация:  ◀ s02e03 — диагностика   │   Season 2   │   s02e05 — подмена DNS ▶
Season README: ../README.md    Курс: ../../README.md
```
