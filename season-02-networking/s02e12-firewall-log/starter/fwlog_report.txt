# Что показал журнал фаервола — s02e12 (ШАБЛОН)
#
# Заполни значения справа от «=». Пробелы вокруг «=» не важны.
#
# Объекты разведки:
#   ../data/ufw_log_moscow1.txt      — журнал за 14–16 октября
#   ../data/ufw_status_moscow1.txt   — правила, действовавшие в это время
#
# Проверка:  bash tests/test.sh

# --- Сколько и чего ----------------------------------------------------------
# записей [UFW BLOCK] — отброшено правилом
blocked=
# записей [UFW LIMIT BLOCK] — отброшено ограничением частоты
limit_blocked=
# записей [UFW ALLOW] — пропущено
allowed=
# сколько разных адресов встречается в блокировках
unique_sources=

# --- Кто и куда --------------------------------------------------------------
# адрес с наибольшим числом блокировок
top_source=
# сколько раз он заблокирован
top_source_hits=
# порт назначения, по которому стучались чаще всего
top_port=

# --- Ограничение частоты ------------------------------------------------------
# порт, который защищён ограничением частоты
limit_port=
# адрес, по которому оно сработало
limit_source=

# --- Сканирование VPN ---------------------------------------------------------
# протокол, которым сканировали порт WireGuard
vpn_scan_proto=
# адрес сканера
vpn_scan_source=

# --- Тот, кого пропустили -----------------------------------------------------
# адрес, чьи пакеты не блокировались, а пропускались
allowed_source=
# правило из ufw status, по которому он проходит (строка «To Action From»: поле From)
allowed_by_rule=

# --- Атакующий ---------------------------------------------------------------
# адрес, знакомый по расследованию Season 1
attacker=
# время его ПОСЛЕДНЕЙ записи в журнале (в формате журнала: «Oct 14 21:57:35»)
attacker_last_seen=
