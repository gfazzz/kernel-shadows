# board_report.txt — паспорт неизвестной платы (СТАРТЕР)
#
# Источники — всё в data/:
#   cpuinfo_node7.txt      что процессор рассказывает о себе
#   devicetree_node7.txt   что ядро знает о железе (дерево устройств)
#   uname_node7.txt        какое ядро и какая архитектура на плате
#   build_host.txt         рабочая машина Макса и собранный им бинарник
#   arm_cpu_parts.txt      справочник ARM: номер ядра -> имя
#
# Каждое значение добывается командой, а не памятью. Формат: ключ=значение,
# без пробелов вокруг «=». Строки с # — комментарии.
#
# Правь копию в artifacts/, потом проверяй:  bash tests/test.sh

# ─────────────────────────────────────────────────────────────────
# 1. Что это вообще за плата
# ─────────────────────────────────────────────────────────────────

# Модель платы — как её называет само дерево устройств (строка целиком).
model=

# SoC — микросхема, на которой всё держится (поле Hardware).
soc=

# Серийный номер платы. Он же — единственный способ отличить
# эту плату от такой же соседней.
serial=

# Архитектура процессора платы (не твоего ноутбука!).
board_arch=

# Сколько процессорных ядер видит ядро Linux.
cores=

# ─────────────────────────────────────────────────────────────────
# 2. Какое там ядро процессора
# ─────────────────────────────────────────────────────────────────

# Номер части процессора (CPU part) — так ARM обозначает модель ядра.
cpu_part=

# Имя ядра по справочнику: implementer + part -> имя (data/arm_cpu_parts.txt).
cpu_core=

# ─────────────────────────────────────────────────────────────────
# 3. compatible: от частного к общему
# ─────────────────────────────────────────────────────────────────

# Первая строка списка compatible корня дерева — самое частное описание.
compatible_specific=

# Последняя строка того же списка — самое общее описание.
compatible_generic=

# ─────────────────────────────────────────────────────────────────
# 4. Что на плате включено
# ─────────────────────────────────────────────────────────────────

# Состояние узла GPIO (значение status).
gpio_status=

# Состояние узла SPI. Прежде чем паять — посмотри сюда.
spi_status=

# Сколько узлов внутри soc/ имеют status = okay.
buses_okay=

# Консоль из bootargs (значение параметра console= целиком).
# Это то, куда плата говорит, когда монитора нет.
console=

# ─────────────────────────────────────────────────────────────────
# 5. Почему собранный бинарник не запустится
# ─────────────────────────────────────────────────────────────────

# Архитектура рабочей машины (uname -m на ноутбуке).
host_arch=

# Архитектура собранного build/collector (как её называет file).
binary_arch=

# Запустится ли этот бинарник на плате? yes или no.
runs_on_board=

# Префикс кросс-компилятора, которым надо было собирать (из -dumpmachine).
cross_prefix=
