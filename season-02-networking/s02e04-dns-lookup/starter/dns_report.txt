# ОТЧЁТ РАЗВЕДКИ — s02e04 «Кто отвечает на вопрос где»
# Объект: season-02-networking/data/dig_capture_ops.txt
#         (записанные ответы dig; сеть не нужна — ответы уже получены)
#
# Заполни значения справа от "=". Имена пиши ровно так, как в файле,
# вместе с завершающей точкой. Списки — через запятую без пробелов.
# Строки с # игнорируются.
#
# Файл разбит на секции строками вида:
#   ;; ===== dig <домен> <тип> =====
# Отвечай по нужной секции, а не поиском по всему файлу: адрес 10.50.1.100
# встречается в двух местах, и это не ошибка.

# --- секция: dig ops.internal A --------------------------------------------
# Все адреса из ответа, по возрастанию последнего октета
a_records=

# --- секция: dig ops.internal MX -------------------------------------------
# Сколько записей MX в ответе
mx_count=
# Имя сервера, к которому отправитель обратится ПЕРВЫМ
mx_primary=

# --- секция: dig ops.internal NS -------------------------------------------
# Сколько серверов имён у зоны
ns_count=

# --- секция: dig ops.internal TXT ------------------------------------------
# Последнее слово в SPF-записи — политика для всех остальных отправителей
spf_policy=

# --- секция: dig www.ops.internal A ----------------------------------------
# На какое имя указывает CNAME
www_cname_target=

# --- секция: dig shadow-05.ops.internal A ----------------------------------
# Адрес в ответе и его TTL
shadow05_ip=
shadow05_ttl=

# --- ДВА РАЗНЫХ «НЕТ» -------------------------------------------------------
# В файле есть два запроса без адреса в ответе, и это РАЗНЫЕ ситуации.
# Разберись, чем они отличаются — это главное в серии.
#
# Имя, для которого сервер ответил NXDOMAIN (домена не существует):
nxdomain_name=
# Тип записи, по которому пришёл NOERROR, но ноль ответов
# (имя существует, записи такого типа у него нет):
noerror_empty_type=
