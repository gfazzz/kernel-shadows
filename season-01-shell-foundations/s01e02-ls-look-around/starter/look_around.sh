#!/usr/bin/env bash
#
# look_around.sh — s01e02 «Осмотреться» (СТАРТЕР)
#
# Задача: «включить свет» в директории — показать ВСЁ содержимое,
# включая скрытые файлы (те, что начинаются с точки).
#
# Как проходить:
#   1. cp starter/look_around.sh ./look_around.sh
#   2. заменить TODO
#   3. bash tests/test.sh
#
# Требования среды: bash + coreutils, без root. Критерии — в mission.md.

set -euo pipefail

target="${1:-.}"

echo "Осматриваю: ${target}"

# TODO 1: покажи ВСЁ содержимое target, включая скрытые файлы и детали.
#         Подумай, какой флаг ls показывает скрытые (начинающиеся с точки),
#         а какой — детали (права, размер, дату).
# ls ...

# TODO 2 (бонус): посчитай и выведи число скрытых объектов (кроме . и ..).
