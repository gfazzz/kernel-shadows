# cluster_report.txt — что на самом деле происходит в кластере (ЭТАЛОН)
#
# Над каждым значением — как оно добыто.

# ─────────────────────────────────────────────────────────────────
# 1. Обзор: сколько чего
# ─────────────────────────────────────────────────────────────────

# Строки таблицы без шапки и комментариев.
pods_total=7

# awk '$3=="Running"' — статус, а не готовность.
pods_running=4

# READY вида x/y, где x == y.
pods_ready=3

# Running, но READY 0/1. Один из четырёх «работающих» на самом деле
# не принимает трафик: контейнер запущен, проба готовности не проходит.
running_not_ready=1

# Все четыре узла Ready — дело не в узлах.
nodes_ready=4

# ─────────────────────────────────────────────────────────────────
# 2. Кто не поднялся и почему — у каждого своя причина
# ─────────────────────────────────────────────────────────────────

# STATUS=Pending, NODE=<none>: планировщик не нашёл, куда его положить.
pending_pod=aurora-api-7d4f8b6c9-f5m8q

# «3 Insufficient memory» — свободной памяти нет ни на одном рабочем узле.
# Четвёртый узел — control-plane, он под taint и не в счёт.
pending_shortage=memory

# ImagePullBackOff: образ не скачался.
imagepull_pod=aurora-worker-5c8d94f7b-h2n6x

# «manifest tagged by "v1.9.3" is not found» — тега нет в реестре.
# Опечатка в версии или образ не собрали.
imagepull_tag=v1.9.3

# CrashLoopBackOff: контейнер стартует и сразу падает.
crashloop_pod=aurora-api-7d4f8b6c9-dk3np

# Exit Code 1 — приложение завершилось само с ошибкой,
# а не было убито системой.
crashloop_exit_code=1

# Из лога предыдущего запуска: не задан ключ конфигурации.
crashloop_missing_key=AURORA_DB_DSN

# ─────────────────────────────────────────────────────────────────
# 3. Тот, кто «работает»
# ─────────────────────────────────────────────────────────────────

max_restarts_pod=aurora-api-7d4f8b6c9-c9v7t

# Сорок семь перезапусков за три часа — примерно раз в четыре минуты.
max_restarts_count=47

# Last State: Terminated, Reason: OOMKilled — его убила система
# за превышение предела памяти.
restart_reason=OOMKilled

# 137 = 128 + 9: процесс получил SIGKILL. Это НЕ ошибка приложения.
restart_exit_code=137

# Предел, из-за которого его убивают.
memory_limit=256Mi

# Проба готовности получает 503: приложение поднялось, но себя
# готовым не считает — не успевает прогреться между убийствами.
readiness_http_code=503

# ─────────────────────────────────────────────────────────────────
# 4. Что это значит для приложения
# ─────────────────────────────────────────────────────────────────

desired_replicas=5

# Deployment честно сообщает: доступны две из пяти.
available_replicas=2

# И столько же реально принимают трафик. Панель, считающая поды
# «по статусу Running», показала бы три — на одну больше, чем есть.
serving_replicas=2
