# ОТЧЁТ РАЗВЕДКИ — s01e02
# Объект: season-01-shell-foundations/data/test_environment ("сервер" Виктора)
#
# Заполни значения справа от "=". Числа — цифрами, имена — через запятую
# без пробелов, в алфавитном порядке. Строки, начинающиеся с #, игнорируются.
#
# Всё выясняется командами. Ни одного значения не нужно угадывать.

# Сколько объектов в корне "сервера" (файлы + каталоги), НЕ считая . и ..
total_entries=2

# Сколько из них скрытых (имя начинается с точки)
hidden_entries=1

# Сколько среди них каталогов
dirs=1

# Имена скрытых объектов в корне, через запятую (например: .foo,.bar)
hidden_names=.next_server

# Сколько объектов внутри каталога documents/, НЕ считая . и ..
documents_entries=2

# Имя скрытого файла внутри documents/
documents_hidden_name=.secret_location
