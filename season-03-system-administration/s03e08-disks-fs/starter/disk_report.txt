# Отчёт по дисковому состоянию shadow-server-01 — s03e08 (ШАБЛОН)
#
# Заполни значения справа от «=». Размеры пиши ровно так, как их печатают
# команды: 18G, 980M, 4.2G — без пересчётов.
#
# Объект разведки: ../data/disk_shadow-01.txt (четыре команды в разделах)
#
# Проверка:  bash tests/test.sh

# --- Где кончается место -----------------------------------------------------
# точка монтирования с наибольшим процентом занятости
fullest_fs=
# её процент
fullest_fs_use=

# --- Где кончилось не место --------------------------------------------------
# точка монтирования, на которой нельзя создать файл, хотя место есть
inode_full_fs=
# сколько там свободного места
inode_full_free=

# --- Расхождение -------------------------------------------------------------
# сколько занято на /var/log по df
var_log_df_used=
# сколько насчитал du по тому же каталогу
var_log_du_total=

# удалённый файл, объясняющий разницу
deleted_file=
# PID процесса, который его держит
deleted_holder_pid=
# имя этого процесса
deleted_holder_command=
# его размер в байтах
deleted_file_bytes=

# --- Что занимает место ------------------------------------------------------
# самый крупный подкаталог /var/log
biggest_subdir=
# его размер
biggest_subdir_size=

# --- Устройство --------------------------------------------------------------
# устройство, смонтированное в /var/log
var_log_device=
# тип файловой системы на нём
var_log_fstype=
# сколько логических томов LVM в системе
lvm_volumes=
