# Что показал разбор образа — s04e04 (ШАБЛОН)
#
# Заполни значения справа от «=».
#
# Объект разведки: ../data/docker_ops_snapshot.txt (пять команд в разделах)
#
# Проверка:  bash tests/test.sh

# --- Что вообще лежит на машине ----------------------------------------------
# сколько РАЗНЫХ образов (по идентификатору, а не по строкам)
distinct_images=
# самый крупный образ (repository:tag)
largest_image=
# его размер, как напечатан
largest_size=

# --- Слои ops/collector -------------------------------------------------------
# сколько слоёв в истории образа
layers=
# инструкция СБОРКИ, давшая самый крупный слой (слои базы помечены #(nop)
# и не в счёт: их писали не вы)
biggest_build_instruction=
# размер этого слоя
biggest_build_size=

# --- Что попало внутрь --------------------------------------------------------
# файл, который скопировали, а потом удалили следующей инструкцией
deleted_file=
# уменьшился ли образ от этого удаления (yes/no)
delete_freed_space=
# переменная окружения, которой там быть не должно
secret_env_var=
# её значение
secret_env_value=

# --- От кого работает ---------------------------------------------------------
# пользователь из inspect: если поле пустое — от кого на самом деле?
effective_user=

# --- База ---------------------------------------------------------------------
# базовый образ с тегом, как записан в Dockerfile
base_image=
# сколько критических уязвимостей в собранном образе
critical_vulns=
# сколько их было бы у базы 3.11-slim
critical_vulns_slim=
