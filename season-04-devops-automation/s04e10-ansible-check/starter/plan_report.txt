# plan_report.txt — что изменится на пятидесяти серверах (СТАРТЕР)
#
# Источник — один файл: data/ansible_check_prod.txt.
# Это снимок прогона `ansible-playbook --limit production --check --diff`:
# ничего не менялось, ansible только рассказал, что бы он сделал.
#
# Каждое значение добывается командой, а не глазами. Формат: ключ=значение,
# без пробелов вокруг знака равенства. Строки с # — комментарии.
#
# Правь копию в artifacts/, потом проверяй:  bash tests/test.sh

# ─────────────────────────────────────────────────────────────────
# 1. Масштаб прогона
# ─────────────────────────────────────────────────────────────────

# Сколько хостов перечислено в PLAY RECAP.
hosts_in_recap=

# Сколько из них ansible не смог достать.
unreachable_hosts=

# На скольких хостах хоть что-то изменилось бы.
changed_hosts=

# Сколько хостов уже совпадают с описанием: ничего не изменится,
# и при этом они доступны.
unchanged_hosts=

# Сколько всего задач в прогоне.
tasks_total=

# ─────────────────────────────────────────────────────────────────
# 2. Где режим проверки молчит
# ─────────────────────────────────────────────────────────────────

# Сколько задач пропущено на ВСЕХ доступных хостах.
tasks_skipped_all=

# Модуль, которым написаны эти задачи (короткое имя, без ansible.builtin.).
# Смотри фрагмент playbook в конце снимка.
blind_module=

# ─────────────────────────────────────────────────────────────────
# 3. Что меняется чаще всего
# ─────────────────────────────────────────────────────────────────

# Задача, которая меняется на наибольшем числе хостов.
# Только имя после двоеточия: из «TASK [common : ntp]» — «ntp».
top_task=

# На скольких хостах она меняется.
top_task_changed=

# ─────────────────────────────────────────────────────────────────
# 4. Что меняется не у всех
# ─────────────────────────────────────────────────────────────────

# Хост, на котором изменилось бы больше задач, чем на любом другом.
# Полное имя, как в PLAY RECAP.
drift_host=

# Сколько задач на нём изменится.
drift_tasks=

# ─────────────────────────────────────────────────────────────────
# 5. Что именно там не так
# ─────────────────────────────────────────────────────────────────

# Комментарий ключа, который ansible убрал бы из authorized_keys
# (последнее поле строки, начинающейся с минуса).
unknown_key_comment=

# Директива sshd_config, значение которой на этом хосте отличается
# от описанного (только имя директивы).
sshd_directive=

# Текущие права на /etc/ssl/private, которые ansible исправил бы.
ssl_mode_current=
