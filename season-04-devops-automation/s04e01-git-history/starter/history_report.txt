# Что рассказала история репозитория — s04e01 (ШАБЛОН)
#
# Заполни значения справа от «=». Короткие хеши — ровно 7 символов
# (`git rev-parse --short=7` или первые 7 из `git log --oneline`).
#
# Репозиторий восстанавливается одной командой и всегда получается
# одинаковым — включая хеши:
#   bash ../data/build_shadow_iac.sh /tmp/shadow_iac
#   cd /tmp/shadow_iac
#
# Проверка:  bash tests/test.sh

# --- Объём и авторы ----------------------------------------------------------
# всего коммитов в текущей ветке, считая слияния
total_commits=
# то же без слияний
commits_no_merges=
# сколько разных авторов
authors=
# автор с наибольшим числом коммитов (имя как в git log)
top_author=
# сколько у него коммитов
top_author_commits=

# --- Опорные точки -----------------------------------------------------------
# короткий хеш самого первого коммита
first_commit=
# его дата в виде ГГГГ-ММ-ДД
first_commit_date=
# коммит, ОТ КОТОРОГО отошла ветка monitoring
branch_point=
# коммит слияния этой ветки
merge_commit=
# сколько файлов отслеживается сейчас
files_tracked=

# --- Инцидент ----------------------------------------------------------------
# файл, в котором в репозиторий попал пароль
secret_file=
# коммит, где он появился
secret_added_in=
# коммит, где его убрали из репозитория
secret_removed_in=
# сам пароль — достань его из истории
secret_password=

# --- Прочее ------------------------------------------------------------------
# коммит с наибольшим числом изменённых строк
largest_commit=
