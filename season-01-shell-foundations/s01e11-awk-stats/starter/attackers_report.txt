# ОТЧЁТ ФОРЕНЗИКИ — s01e11 «Кто стучался чаще всех»
# Объект: season-01-shell-foundations/data/access.log
#
# Заполни значения справа от "=". Числа — цифрами, адреса и пути — как в журнале,
# списки — через запятую без пробелов. Строки с # игнорируются.
#
# Формат строки (Apache Combined):
#   IP - - [дата] "МЕТОД /путь HTTP/1.1" статус размер "referer" "user-agent"
#    $1                                              $9 — но ТОЛЬКО пока
#                                                    в запросе ровно два пробела.

# Сколько РАЗНЫХ адресов встречается в журнале
unique_ips=

# Адрес с наибольшим числом запросов и само число
top_ip=
top_ip_requests=

# Три самых активных адреса по убыванию, через запятую
top3_ips=

# Самый запрашиваемый путь (второе поле внутри кавычек запроса)
top_url=

# --- ДВА ЗНАЧЕНИЯ ОДНОЙ ВЕЛИЧИНЫ -------------------------------------------
# Ниже нужно посчитать статус 403 ДВУМЯ способами и записать оба.
# Они не совпадут. Разберись почему — это и есть главное в серии.
#
#   by_field9:  awk '{print $9}' access.log | grep -c '^403$'
#   correct:    сначала отрезать запрос по кавычкам, потом взять первое поле:
#               awk -F'"' '{print $3}' access.log | awk '{print $1}' | grep -c '^403$'
status_403_by_field9=
status_403_correct=

# Сколько РАЗНЫХ статусов в журнале, если считать правильным способом
distinct_statuses=

# --- Минута пика: 04/Oct/2025:03:47 ----------------------------------------
# Самый активный адрес внутри этой минуты и число его запросов
peak_top_ip=
peak_top_ip_requests=
