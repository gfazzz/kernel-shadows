# ОТЧЁТ ФОРЕНЗИКИ — s03e01 «Кто есть на этой машине»
# Объект: season-03-system-administration/data/passwd_shadow-01.txt
#         season-03-system-administration/data/group_shadow-01.txt
#         season-03-system-administration/data/shadow_shadow-01.txt
#
# Заполни значения справа от "=". Числа — цифрами, логины — через запятую
# без пробелов, в алфавитном порядке. Строки с # игнорируются.
#
# Все три файла начинаются с комментариев — их надо отбросить перед разбором:
#   grep -vE '^[[:space:]]*(#|$)' "$P"
#
# Формат /etc/passwd:  логин:x:UID:GID:комментарий:дом.каталог:оболочка
# Формат /etc/group:   имя:x:GID:список дополнительных членов
# Формат /etc/shadow:  логин:хеш:…  (см. шапку самого файла)

# Всего учётных записей в /etc/passwd
accounts_total=

# Системных (UID меньше 1000)
system_accounts=

# Человеческих (UID 1000 и больше, кроме nobody с UID 65534)
human_accounts=

# ВСЕ учётные записи с UID 0, через запятую.
# Их должно быть ровно одна. Если больше — вторая и есть способ входа
# с полными правами мимо всякого sudo.
uid0_accounts=

# Кто перечислен в группе sudo (четвёртое поле строки группы), через запятую
sudo_members=

# Учётные записи, у которых поле пароля в /etc/shadow ПУСТОЕ,
# то есть вход возможен вообще без пароля
passwordless=

# Заблокированные учётные записи (поле пароля начинается с "!")
locked_accounts=

# Системные учётки (UID < 1000) с ИНТЕРАКТИВНОЙ оболочкой (bash, sh, zsh),
# КРОМЕ root. Через запятую.
# Осторожно: «не nologin» и «интерактивная» — разные вещи. У учётки sync
# оболочка /bin/sync: она не nologin, но войти в систему через неё нельзя.
service_with_shell=

# Сколько человек имеют доступ к логам через группу ops-logs
ops_logs_members=

# Одним логином: учётная запись, которой на этой машине быть не должно.
# Признак — полные права root при том, что заводили её не вы.
backdoor_account=
