# ВАЖНО: этот файл — ОДНА строка. Ядро читает только первую строку;
# всё, что ниже, молча игнорируется. Комментарии здесь тоже не поддер-
# живаются — эти строки надо удалить целиком.
#
# Собрать строку параметров ядра для узла shadow-node-07:
#   console=      куда ядро печатает (скорость — из data/board_facts.txt)
#   root=         корневой раздел по PARTUUID (оттуда же)
#   rootfstype=   тип файловой системы
#   rootwait      дождаться появления носителя, а не гадать
#   fsck.repair=  чинить файловую систему без вопросов — спрашивать некого
